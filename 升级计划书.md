# 灵梦work（lingmengwork）全面升级计划书

> 版本：v0.2.0 → 目标基线（待确认是否升 0.3.0）
> 编写日期：2026-08-27
> 模式：计划先行，确认后分段执行

---

## 0. 现状盘点（基线）

### 0.1 项目概况
| 项 | 值 |
|---|---|
| 名称 | 灵梦work（lingmengwork）— 本地优先 AI 编码代理工作台 |
| 当前版本 | `VERSION = 0.2.0`（android_app/pyproject.toml 同步 0.2.0） |
| 入口 | `lingmengwork_launcher.py` → `lingmengwork` 包 |
| 分支 / HEAD | `master` / `dcb9833`（feat(lib): 新增 lmw_lib 零依赖通用 Python 工具包） |
| 测试规模 | `tests/` 下 60+ 个 `test_*.py`；最近一次跑批出现 **2 个 F**（待 Phase 1 复跑定位） |
| 默认 LLM 后端 | 商汤 SenseNova（OpenAI 兼容），key 走环境变量 |

### 0.2 待升级资产清单（当前未提交）
**新增模块（个人工作区能力）**
- 核心 7 个：`enhance.py` `backup.py` `notes.py` `secrets.py` `snippets.py` `templates.py` `todos.py`
- 工具层 8 个：`tools/backup_tools.py` `note_tools.py` `secret_tools.py` `snippet_tools.py` `template_tools.py` `todo_tools.py` `office.py` `dev.py`
- 前端页 7 个：`web/static/{backup,notes,secrets,snippets,templates,todos,enhance}.html`
- 数据：`prompts_enhance.json`
- 已修改 6 个：`agent/loop.py` `llm/client.py` `tools/registry.py` `web/server.py` `web/static/{app.js,index.html,styles.css}`

### 0.3 关键风险与缺口
1. **构建一致性缺口（P1）**：`lingmengwork.spec` 的 `hiddenimports` 未包含上述 7 个核心模块及其工具层。新模块在 `web/server.py` 内以 `from .. import X` 懒加载——PyInstaller 字节码扫描通常能捕获，但 spec 为手动维护清单，建议补齐以防冻结 exe 漏包。
2. **工作区污染（P1，低风险）**：`build/` 与 `dist/` 下累计 20+ 个 `.bak_*` 历史构建目录；项目根散落 `e2e_*.txt` `diag*.txt` `pytest_out.log` `tests_result.log` `.lmw_audit.log` 等临时文件（均已被 `.gitignore` 排除，但物理占用且干扰巡检）。
3. **测试未全绿（P0）**：最近跑批 2 个用例失败，需定位修复或标注已知。
4. **版本未推进（P1，可选）**：pending 批次含大量新能力，`VERSION` 仍 0.2.0，建议升 0.3.0 并同步 android 端。
5. **依赖可审计性（P2）**：主程序无 `requirements.txt`（疑似纯标准库 + 少量三方）；`android_app` 锁定 `briefcase==0.3.20`（偏旧）；`reference/opencode/go.mod` 为空文件。

### 0.4 已确认安全项
- `.env` 已被 `.gitignore` 排除，密钥未入库；代码中无硬编码密钥（仅 `config.toml` 的 `api_key` 格式化输出，非明文）。
- `.gitignore` 已覆盖 `dist/ build/ *.bak_* *.log e2e*.txt diag*.txt rep_body*.txt` 等，误提交风险低。

---

## 1. 升级总目标
将项目从「含大量未提交新功能 + 构建/工作区污染」的状态，升级为 **测试全绿、构建一致、仓库整洁、版本明确、可安全发布** 的稳定基线，并据确认决定是否重建 exe / 提交 / 升版本。

---

## 2. 阶段划分与执行顺序

### 阶段 0 — 基线固化（约 0.5h）
- 记录当前 `git HEAD`、待提交文件清单、当前测试通过率（复跑 pytest 取基线）。
- 产出：基线快照（附于本计划书末）。

### 阶段 1 — 测试与质量基线（P0，必做）
- 在项目根以 venv python 跑完整 `pytest`，记录通过/失败清单。
- 定位并修复 2 个失败用例（或确认属已知可接受并显式标注）。
- 对所有新增 `.py` 做 `py_compile` 语法核验 + 排查遗留 debug 代码（`print`/`breakpoint`/`pdb`/TODO-FIXME）。
- 校验新工具已在 `registry.py` 正确注册（`backup_list` `template_*` `secret_*` `snippet_*` `note_*` `todo_*` 已在 `_READONLY_TOOLS`）。
- **验收**：pytest 全绿（或失败已 documented）；所有新模块可正常 import。

### 阶段 2 — 构建一致性加固（P1）
- 在 `lingmengwork.spec` 的 `hiddenimports` 补齐：`enhance` `backup` `notes` `secrets` `snippets` `templates` `todos` 及其 `tools.*` 模块。
- 校验 `datas` 已含 `config.toml` / `VERSION` / `web/static`（已含，确认即可）。
- （可选，须用户批准）做一次临时验证构建确认无 `ModuleNotFoundError`；**按约定默认不重建发布版 exe**。
- **验收**：spec 与新模块一致；若批准重建则 exe 启动 + `/api/meta` 自检通过。

### 阶段 3 — 仓库与工作区清理（P1，必做，低风险）
- 清理 `build/` 与 `dist/` 下的历史 `.bak_*` 目录（保留最近一份可用构建作回滚）。
- 删除项目根临时文件：`e2e_*.txt` `diag*.txt` `diag2.txt` `rep_body*.txt` `pytest_out.log` `tests_result.log` `.lmw_audit.log` `_dev_*.py` `dev_spawn_test.py`。
- 复核 `.gitignore` 覆盖度，确认无密钥/产物可被误提交。
- ⚠️ 清理一律用 **Python `shutil`/`os.remove`**（本环境 safe-delete 垫片拦截 Node/rm，bash `rm` 静默失效）；`.bak_*` 先移动到 `/tmp` 备份再删，可留作回滚。
- **验收**：`git status` 仅剩本次升级相关改动；工作区无散落临时文件。

### 阶段 4 — 依赖与安全审计（P2）
- 扫描主程序三方依赖（`import` 清单），对存在的包做 `pip-audit` / 版本就绪检查；纯标准库则标注。
- `lmw_lib` 零依赖确认；`android_app` 标记 `briefcase 0.3.20` 偏旧（可选升级建议，不强行改）。
- `reference/opencode/go.mod` 空文件标记（参考材料，非发布物）。
- 复检密钥：`.env` 不入库、代码无硬编码（已确认干净）。
- **验收**：依赖清单明确，无高危漏洞，安全项复核通过。

### 阶段 5 — 收尾与交付（按确认执行）
- A. **提交 pending 功能批次**：结构化 commit（`feat(workspace): 个人工作区能力 snippets/templates/todos/notes/secrets/backups/enhance`）。
- B. **版本推进**：`VERSION → 0.3.0`，同步 `android_app/pyproject.toml`，README 增补新能力说明。
- C. （可选）**重建 exe 发布**（须用户明确批准，遵循「默认不重打包」约定）。
- D. 产出升级小结 / CHANGELOG 片段。
- **验收**：仓库可达发布态；如提交则 push 前由用户贴 PAT。

---

## 3. 任务分解总表（执行顺序）

| 序 | 阶段 | 任务 | 优先级 | 风险 |
|---|---|---|---|---|
| 1 | 0 | 复跑 pytest 取基线 + 记录 HEAD/文件清单 | P0 | 低 |
| 2 | 1 | 修复/标注 2 个失败用例 | P0 | 中 |
| 3 | 1 | 新增模块 `py_compile` + 去 debug 代码 | P0 | 低 |
| 4 | 1 | 校验 registry 工具注册 | P0 | 低 |
| 5 | 2 | spec `hiddenimports` 补齐新模块 | P1 | 低 |
| 6 | 2 | （可选）临时验证构建 | P1 | 中 |
| 7 | 3 | 清理 build/dist `.bak_*` | P1 | 低 |
| 8 | 3 | 删除根目录临时文件 | P1 | 低 |
| 9 | 3 | 复核 `.gitignore` 覆盖 | P1 | 低 |
| 10 | 4 | 主程序依赖扫描 + pip-audit | P2 | 低 |
| 11 | 4 | lmw_lib / android / go.mod 标记 | P2 | 低 |
| 12 | 5 | 提交 pending 批次（如确认） | P1 | 低 |
| 13 | 5 | 版本升 0.3.0 + 同步安卓端（如确认） | P1 | 低 |
| 14 | 5 | 产出升级小结 / CHANGELOG | P1 | 低 |

---

## 4. 关键决策点（需用户确认）
- **D1** 是否重建 exe（默认 **否**，遵循不重打包约定）
- **D2** 是否提交 pending 功能批次（建议 **是**）
- **D3** 是否 bump 版本到 0.3.0（建议 **是**）
- **D4** 清理力度：保留最近回滚备份 vs 全清（建议 **保留最近一份**）

---

## 5. 风险与回滚
- **清理误删**：`.bak_*` 先 `mv` 到 `/tmp` 备份再删；git 改动可 `git stash` / `git checkout` 回滚。
- **测试修复引入回归**：每修一例重跑相关用例。
- **重建 exe 失败**：保留上一可用 `dist`（已 `.bak`），不覆盖。

---

## 6. 预计产出
- 本升级计划书
- 测试基线报告（通过/失败清单）
- 更新后的 `lingmengwork.spec`
- 整洁工作区 + 干净 git 状态
- （可选）新 commit / 0.3.0 版本 / 重建 exe

---

### 附录：基线快照（阶段 0 填写）
- git HEAD：`dcb9833`
- 待提交文件数：约 29（修改 6 + 新增 23）
- pytest 基线：待阶段 0 复跑填写
- 新增模块 LOC 合计：约 868（核心 7 个）
