# 灵梦work 全量升级总纲 · Phase 20–30

> 从「本地 AI 编码工作台」跃迁为「综合超级 AGENT OS」
> 作者视角：顶级系统架构师 + 智能体 OS 设计者
> 配套文档：`灵梦work_多模态域_音频图文视频.md` · `灵梦work_工作伙伴与超级AGENT.md`

---

## 0. 战略定位（一句话）

**灵梦work = 运行在你本机的、可自进化、可审计、隐私优先的综合超级 AGENT OS。**
编码只是它第一个被打通的能力域；音频、图像、视频、工作伙伴是并列的能力域；超级 AGENT 内核是统一编排这一切的「大脑」。

内核契约（零依赖 / local-first / 可审计 / 可自愈 / 可自主进化 / 隐私优先）**不可动摇**——所有新能力都长在这套契约之上，而不是另起炉灶。

---

## 1. 六大能力域矩阵

| 能力域 | 现状 | 全量升级目标 | 关键产物 |
|---|---|---|---|
| **编码** (基石) | 工具治理/循环推理/语义检索/结构化决策/可观测已落地 | 行式工具协议硬化 + 多工具链路 + 真 LLM 评审闭环 + 代码资产沉淀 | `loop.py` / `tools/*` / `review.py` 增强 |
| **音频** (新) | 仅幻境契约局部音效 | TTS 语音合成 + 语音克隆 + 音乐生成 + 音效合成 + 音频剪辑 | `audio_engine.py` + 资产库 audio 类 |
| **图像** (新) | 仅对话内文生图降级 | 文生图 + 图生图 + Inpainting + 超分 + 资产入库 | `image_engine.py` + 资产库 image 类 |
| **视频** (新) | 仅降级 GIF | 文生视频 + 图生视频 + 剪辑/字幕/配音 | `video_engine.py` + 资产库 video 类 |
| **工作伙伴** (新) | 单 AGENT 循环 | 多智能体联邦：编码/创作/研究/运维伙伴分工协作 | `federation.py` + 派发协议 |
| **超级 AGENT 内核** (新) | 单路 `_chat_sse` | 统一目标理解→域路由→并行编排→收敛→自检→记忆沉淀 | `superagent.py` + `memory_graph.py` |

---

## 2. 不可变内核契约（任何升级不得违反）

1. **零依赖内核**：`loop.py` / `tools/` / `memory` / `selfcheck` / `self_heal` 保持零三方依赖，纯标准库。
2. **local-first**：所有数据默认落本机；云能力（文生图/视频 key）为可选外挂，缺失时走本地降级链。
3. **可审计**：每次工具调用/域调度/自愈动作均进事件总线 + JSONL 审计链。
4. **可自愈**：失败信号聚合 → 结构化预案 → （2.0）LLM 生成 diff + 沙箱验证门 → 人工合并。
5. **可自主进化**：记忆图谱沉淀经验，下一轮目标复用历史决策。
6. **隐私优先**：真实密钥只走 `.env` / 环境变量，不进仓库、不进 exe、不进日志明文。

---

## 3. 四层架构（升级后）

```
┌─────────────────────────────────────────────────────────────┐
│  交互层  Nebula 面板 / CLI / OpenAPI（/api/chat /tasks /heal…） │
├─────────────────────────────────────────────────────────────┤
│  超级 AGENT 内核  superagent.py                               │
│   目标理解 → 域路由 → 并行编排 → 收敛 → 自检 → 记忆沉淀        │
├─────────────────────────────────────────────────────────────┤
│  能力域层                                                     │
│   编码 loop/tools   音频 audio_engine   图像 image_engine      │
│   视频 video_engine   工作伙伴 federation（多智能体）           │
├─────────────────────────────────────────────────────────────┤
│  内核层（不可变契约）                                          │
│   loop · tools registry · memory_graph · event_bus ·          │
│   audit · selfcheck · self_heal · automation_hub              │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 分批路线 Phase 20–30（每批独立交付、e2e 验证）

| Phase | 主题 | 交付物 | 验收门槛（零失败 + 烟测绿） |
|---|---|---|---|
| **20** | 自愈闭环 2.0 | `self_heal` + diff 生成 + 沙箱验证门 + 人工合并 UI | 失败信号→生成可应用 diff→沙箱验证通过→人工合并落盘 |
| **21** | 统一多模态引擎基座 | `multimodal/` 抽象 + 资产库 + 降级链 + `creation_domains` 接入 | 音频/图/视频统一接口跑通；无 key 降级不崩 |
| **22** | 音频域 | `audio_engine.py`：TTS/音乐/音效 + 资产入库 | 真实音频产出（有 key）或本地占位（无 key） |
| **23** | 图像域 | `image_engine.py`：文生图/图生图/Inpainting/超分 | 真实 PNG 产出（有 key）或本地占位 |
| **24** | 视频域 | `video_engine.py`：文生视频/图生视频/剪辑配音 | 真实 MP4 产出（有 key）或降级 |
| **25** | 工作伙伴（联邦） | `federation.py`：伙伴注册/派发/协作契约 | 跨伙伴任务派发闭环跑通 |
| **26** | 记忆图谱 | `memory_graph.py`：facts→实体关系→跨会话推理 | 复杂目标召回历史记忆链 |
| **27** | 超级 AGENT 内核 | `superagent.py`：目标路由/并行编排/收敛 | 单目标跨 2+ 域编排成功 |
| **28** | 插件 / 专家市场 | enhance 库导入导出 + 社区共享协议 | 社区库一键安装生效 |
| **29** | 全链路可观测+合规 | 成本/安全/合规看板升级 | 六域调用全被审计覆盖 |
| **30** | 生产化 | PyInstaller 重打包 + 文档 + 压测 | exe 带 Phase 1–30 全部后端上线 |

---

## 5. 每批执行纪律（沿用已验证流程）

```
改动 → 单测(pytest) → 全量回归零失败 → HTTP 烟测 → 蓝图补行 → 提交
```

- 后端先源码 `8320` 验证（`启动源码面板.bat`），**冻结 exe(8318) 不含新后端，重打包需用户许可**。
- 凡调用 `get_hub()` / `get_bus()` 等 cwd 持久化单例的测试，**必须 `monkeypatch.chdir(tmp_path)`** 隔离（已踩坑）。
- 每批收尾 `git log` / `git diff` 复核真实状态，运行时污染文件（`automations.json` / `.lmw_*`）不纳入提交。

---

## 6. 风险与对策

| 风险 | 对策 |
|---|---|
| 文生图/视频 key 缺失导致能力空洞 | 统一降级链：本地占位 + 明确「需配置 key」提示，不阻断主流程 |
| 多域并行编排失控（无限循环/上下文爆炸） | 复用 `loop.py` 收敛护栏（上限 32 轮 + 三级硬护栏）+ 域预算配额 |
| 记忆图谱噪声累积 | facts 带置信度 + 衰减 + 人工标注可信源 |
| 超级 AGENT 黑箱化 | 每次编排留结构化 trace 进审计链，可回放 |
| 重打包体积膨胀 | 多模态依赖隔离为可选 pip  extras，exe 默认不含，运行时按需装 |

---

## 7. 下一步

方案确认后说「**继续**」即进入 **Phase 20 自愈闭环 2.0** 落地（先出 diff 生成 + 沙箱验证门详细设计，再编码/单测/烟测/蓝图补行/提交）。
后续 Phase 21–30 按上表逐批推进，每批可独立交付、可中途停下评审。
