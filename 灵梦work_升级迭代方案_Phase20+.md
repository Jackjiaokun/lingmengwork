# 灵梦work 升级与迭代方案（Phase 20+）

> 顶级架构视角 · 本地优先 · 零依赖内核 · 可审计 · 可自愈 · 可自主进化
> 基线：终极蓝图已落地 Phase 1–19（四引擎 + 统一总控台 + 离线自检 + 实时活动总线 + 操作审计链 + 自主进化闭环 + 结构化补丁预案）。冻结 exe 未重打包，源码 `8320` 跑全量。

---

## 一、战略定位升级

从「本地 AI 编码智能体工作台」升级为 **「可自进化的本地智能体 OS」**：

- **内核契约不变**：零依赖、local-first、可审计、安全可演示（沿用 Phase 1–19 已验证的硬约束）。
- **能力外延**：编码 → 创作 → 多域联邦。
- **自主等级演进**：规划 → 执行 → 观察 → 自检 → **自愈（自动补丁）** → 进化。

这是把"工程信条"从宣言变成可运行闭环的关键一跃。

---

## 二、五大战略升级方向

| # | 方向 | 战略意图 | 与现有基座关系 |
|---|---|---|---|
| 1 | **自愈闭环 2.0** | 从"文本预案"升级为"LLM 生成真实 diff 候选 + 沙箱验证门 + 人工一键合并"（终态仍 human-in-the-loop） | 深化 Phase 18/19 自愈提议 |
| 2 | **生产级多模态** | 真实文生图 / 文生视频（接生成式 API 或本地模型），产物入资产库可检索复用 | 升级 Phase 8/12 模板与降级链路 |
| 3 | **长期记忆图谱** | facts → 实体-关系图谱，跨会话推理与因果链召回 | 升级 Phase 9 语义记忆 |
| 4 | **插件 / 专家市场** | enhance 库从本地 JSON 升级为可导入 / 导出 / 社区共享的插件生态 | 升级 Phase F 专家·技能增强 |
| 5 | **多智能体联邦** | 编码 / 创作 / 研究 / 运维实例分工协作、任务联邦派发 | 升级 Phase 3 多智能体编排 |

---

## 三、迭代方案（Phase 20+ 分批，每批可独立交付、e2e 验证、可"继续"推进）

| Phase | 主题 | 核心交付 | 验收门槛（门禁） |
|---|---|---|---|
| **20** | 自愈闭环 2.0 | LLM 生成补丁 diff + 沙箱 `py_compile`/`pytest` 验证门 + `POST /api/heal/apply` 人工合并（终态仍确认） | 注入失败信号 → 生成可应用 diff → 沙箱验证通过 → 人工一键合并落盘 |
| **21** | 生产级多模态 | 真实文生图 / 文生视频接入 + 资产落 `outputs/` 并入库检索 | 真实 PNG / MP4 产出（非降级 GIF），缺 key 自动降级不崩 |
| **22** | 记忆图谱 | facts → 实体-关系图谱，跨会话推理召回 | 复杂目标召回相关记忆链（非单条 fact） |
| **23** | 插件 / 专家市场 | enhance 库导入 / 导出 / 社区共享安装 | 社区库一键安装并即时生效 |
| **24** | 多智能体联邦（可选） | 实例协作分工 / 联邦派发 | 跨实例任务派发闭环可达 |

每批遵循：**改动 → 单测 → 全量 pytest 零失败 → HTTP 烟测 → 蓝图补行 → 提交**；后端改动先源码 `8320` 验证，需你许可才 PyInstaller 重打包。

---

## 四、部署与纪律

- **重打包节奏**：Phase 20 完成后建议一次性 PyInstaller 重打包，解锁 Phase 1–20 全量后端并重启 `8318` 上线（**需你明确许可**）。
- **预览纪律**：agent 环境无法托管宿主可达服务（实测 PowerShell / 沙箱均落沙箱 netns，宿主 `Get-NetTCPConnection` 看不到监听）；预览靠你双击 **`启动源码面板.bat`**（8320 源码全量）或 **`启动面板.bat`**（8318 冻结 exe，不含 Phase 1–19）。
- **测试纪律**：延续 `conftest._no_real_llm` + `pytest-timeout=120` + 六道门（validate → typecheck → test → smoke → balance → bundle），全量回归零失败才提交。
- **安全边界**：自愈生成的补丁 diff **必须沙箱隔离 + 人工终态确认**，绝不自动改源码。

---

## 五、优先级与里程碑

- **P0（立即）**：**Phase 20 自愈闭环 2.0** — 收口"可自愈"信条的工程闭环，是智能体 OS 的招牌能力。
- **P1**：**Phase 21 多模态生产级** — 补齐真实媒体产能，让创作域从"蓝图"变"作品"。
- **P2**：**Phase 22–24** 长期记忆 / 生态 / 联邦 — 构建长期护城河。

---

## 六、风险与对策

| 风险 | 对策 |
|---|---|
| LLM 生成补丁的安全 | 沙箱验证门（编译 / 单测）+ 人工合并终态，绝不静默落盘 |
| 真实多模态成本 / 密钥 | 保留降级链路（信息图 / GIF / edge_tts），缺 key 不崩 |
| 重打包回归 | 每批源码验证 + 全量 pytest 后再 rebuild，回滚用 `.bak_*` |

---

**下一步**：说"继续"，我从 **Phase 20 自愈闭环 2.0** 开始落地（先出补丁 diff 生成 + 沙箱验证门设计，再编码、单测、烟测、蓝图补行、提交）。
