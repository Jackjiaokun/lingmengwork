# 灵梦work · 终极蓝图（全宇宙顶尖 AI 全能智能体平台）

> **定位**：一款次世代 AI 全能工具，强大的编程 · 音频 · 图片 · 视频 全面 AI Agent 能力。
> **工程信条**：本地优先、零三方依赖内核、可审计、可自愈、可自主进化。
> **本文件**：先出蓝图（战略 + 架构 + 能力矩阵 + 路线图），随后分段连续执行，中途不停。

---

## 一、战略定位与对同业的结构性超越

当前主流 AI 编码/创作工具存在三类结构性短板，本平台以架构优势逐一压制：

| 同业短板 | 灵梦work 的压制性设计 |
|----------|----------------------|
| **单域孤岛**：编码归编码、图像归图像、视频归视频，上下文割裂 | 四大创作域（编程/音频/图片/视频）共享**同一上下文引擎 + 同一记忆 + 同一工具总线**，一次对话可跨域串联（如：写脚本→生成配图→合成配音→剪辑成片） |
| **上下文健忘**：长会话掉上下文、无分层记忆 | 「工作记忆 + 语义长期记忆 + 每日日志 + 跨会话知识库」四级记忆；压缩/整理/拆解三件套主动治理上下文 |
| **被动应答**：你问一句它答一句，不会自己把事干完 | **自主模式（Autonomous）**：目标驱动的自驱循环（规划→执行→观察→反思→修正），带 Critic 回路与自修复 |
| **黑盒不可控**：工具调用不可见、不可干预、出错即崩 | 全链路可观测（实时统计/成本/计划看板）、行级工具协议、破坏性操作护栏、错误自动入库与汇总 |
| **云锁定**：数据出机、依赖外部服务 | 本地优先内核（纯标准库）、LLM 后端可切（本地 Ollama / 云端兼容 / Mock）、MCP 工具总线可扩展 |

**终极目标态**：用户输入一个模糊目标，平台自动完成「理解→拆解→编排→执行→自检→交付」，并在编程/音频/图片/视频四域间自由调度，全程可观测、可干预、可回滚。

---

## 二、现状盘点（地基已就位）

- **内核**：`lingmengwork` CLI + Web 控制台，纯标准库零三方依赖，冻结 exe 可分发。
- **工具总线**：9 个 MCP 服务器（demo/fs/git/fetch/shell/grep/sqlite/search/review）。
- **Web 面板**：Nebula 暗色设计系统 + 四主题（编码/音频/图片/视频）+ mini-IDE（多标签/查找替换/大纲/全局搜索）。
- **记忆与上下文**：`memory_mgr` / `plans` / `context_ops` / `errorlog` 四模块 + 对应页面（本轮前尚待收口提交）。
- **编排**：多路任务 + 并行编排（`/api/tasks` + `/api/orchestrations`）。
- **可观测**：统计/成本/计划三看板 + 结构化结果回写对话流。

---

## 三、目标架构（六边形能力模型）

```
                    ┌─────────────────────────────┐
                    │      统一对话 / 创作入口       │
                    └──────────────┬──────────────┘
                                   │
        ┌──────────────┬───────────┴───────────┬──────────────┐
        ▼              ▼                       ▼              ▼
  ┌─────────┐   ┌────────────┐         ┌────────────┐  ┌────────────┐
  │ 编程域   │   │ 音频域      │         │ 图片域      │  │ 视频域      │
  │ Agent    │   │ Agent       │         │ Agent       │  │ Agent       │
  └────┬────┘   └─────┬──────┘         └─────┬──────┘  └─────┬──────┘
       └──────────────┴─────────┬────────────┴──────────────┘
                                ▼
                 ┌──────────────────────────────┐
                 │   层级任务分解 / 多智能体编排    │
                 │   (目标→子目标→步骤树→路由)      │
                 └──────────────┬───────────────┘
                                ▼
   ┌──────────────┬─────────────────────────────┬──────────────────┐
   ▼              ▼                             ▼                  ▼
┌────────┐  ┌────────────┐              ┌────────────┐     ┌────────────┐
│ 上下文  │  │  语义记忆   │              │  工具总线   │     │  自主/反思  │
│ 引擎    │  │ (四级)      │              │ (MCP×9+)   │     │  闭环        │
└────────┘  └────────────┘              └────────────┘     └────────────┘
                                ▼
                 ┌──────────────────────────────┐
                 │  可观测 / 护栏 / 错误汇总 / 交付 │
                 └──────────────────────────────┘
```

**六条边（必须全部做到顶级）**：上下文治理 · 语义记忆 · 工具可靠 · 编排智能 · 自主闭环 · 可观测可控。

---

## 四、四大创作域能力矩阵

| 域 | 主题色 | 核心 Agent 能力 | 交付闭环 |
|----|--------|------------------|----------|
| 🟣 编程 | 紫青 | 读/写/跑/重构/调试/多文件、工具链、单测、评审 | 代码 + 评审报告 + PR 草稿 |
| 🟢 音频 | 青绿 | 语音合成(TTS)、语音转写(STT)、音效/配乐生成、音频清洗 | 音频文件 + 字幕/脚本 |
| 🌸 图片 | 粉橙 | 文生图、图生图、智能编辑/抠图/标注、版式设计 | 图片集 + 说明 |
| 🔵 视频 | 蓝紫 | 文生视频、脚本→分镜→成片、字幕/配音合成、剪辑封装 | 视频文件 + 工程 |

四域通过**统一创作工作台**串联，共享上下文与记忆，支持跨域流水线（如自动生成带配音与配图的短视频）。

---

## 五、核心引擎深化要点

1. **上下文引擎（context_ops）**：规则版 → **LLM 驱动 + 确定性回退**。compress 提炼摘要、organize 智能分段打标、decompose 生成可执行步骤树。
2. **语义记忆（memory_mgr）**：LLM 抽取关键事实/决策/偏好 → 长期记忆 + 每日日志；提供按查询召回的 retrieval；跨会话去重与衰减。
3. **编排（orchestrate）**：目标→子目标→步骤树的层级分解；依赖图调度、失败重试、结果汇聚。
4. **自主闭环（autonomous）**：目标驱动自驱循环 + Critic 回路 + 自修复；错误自动入错误日志并触发修复尝试。
5. **护栏与可观测**：破坏性操作分级护栏、行级工具协议、实时统计/成本/计划看板、错误汇总。

---

## 六、分阶段路线图（连续执行，中途不停）

- **Phase 0 — 地基收口**：提交遗留的 4 模块 + 4 页面 + server.py；pytest 基线回归。✅ 本阶段首先执行。
- **Phase 1 — 上下文引擎 LLM 化**：compress/organize/decompose 升级为 LLM 驱动 + 回退；增强 `/api/context/*`。
- **Phase 2 — 语义记忆升华**：memory_mgr 四级记忆 + retrieval；前端「智能召回」。
- **Phase 3 — 多智能体编排**：层级任务分解引擎 + `/api/orchestrate` + 编排可视化。
- **Phase 4 — 四大创作域 Agent**：四域注册/路由/统一创作工作台；音频/图片/视频 经 MCP/API 接入。
- **Phase 5 — 自主模式 + 自我反思**：Autonomous 自驱循环 + Critic + 自修复 + 错误自修复闭环。
- **Phase 6 — 收口/重建上线**：全量 pytest + 前端冒烟 + 可观测复核 + 文档同步 + 用户许可后 PyInstaller 重打包验证。

---

## 七、执行纪律与质量门

- **每段可独立验证**：每段结束跑 `node --check` / `py_compile` / 源码服务 curl 烟测，再进入下一段。
- **不破坏既有能力**：保留全部 JS 依赖 ID/类名；新增不覆盖旧功能。
- **部署纪律**：后端/前端改动先源码层验证；冻结 exe 重打包仅在用户许可后执行（避免误上线）。
- **记忆同步**：每段产出写入 `.workbuddy/memory` 当日日志，长期约定入 `MEMORY.md`。
- **中途不停**：本会话连续推进 Phase 0 → Phase 6，每段完成即汇报阶段成果并进入下一段。

---

_本蓝图随执行推进持续更新（进度标注于各 Phase 任务）。_

## 八、执行进度（本会话 · 2026-08-27）

| Phase | 主题 | 状态 | 代表提交 |
|-------|------|------|----------|
| 0 | 地基收口（记忆/计划/上下文/错误日志/技术文档） | ✅ 完成 | 94e1276 |
| 1 | 上下文引擎 LLM 化（压缩/整理/拆解） | ✅ 完成 | 6ad4ab8 |
| 2 | 语义记忆升华（LLM 抽取 + 语义召回） | ✅ 完成 | 9002e51 |
| 3 | 多智能体编排（层级任务分解 + 编排中枢） | ✅ 完成 | 394ed80 |
| 4 | 四大创作域 Agent + 统一创作工作台 | ✅ 完成 | ba0bdd5 |
| 5 | 自主模式（自驱循环 + Critic + 自修复） | ✅ 完成 | 6f90252 |
| 6 | 收口/演练/验证/文档同步 | ✅ 完成（pytest 426 passed；exe 重打包待用户许可） | 0e65632 |
| 7 | 全链路目标驱动流水线（理解→拆解→编排→执行→自检→交付） | ✅ 完成 | 0bd6c06 |
| 8 | 真实多模态适配层（创作域升级为真实媒体交付） | ✅ 完成 | dd170ff |
| 9 | 跨会话记忆喂入流水线（理解带历史偏好 + 运行后写回） | ✅ 完成 | 44dccdb |
| 10 | 统一引擎总控台（四引擎统一可观测 + 一键启动器） | ✅ 完成 | 7ca8e25 |
| 11 | 流水线×自主深度融合（编程域驱动自主回路 + 真实代码生成 + 编译评审门） | ✅ 完成 | 1a2993e |
| 12 | 多模态生成中枢 2.0（image/audio/video 三域 LLM 智能设计驱动真实媒体产出） | ✅ 完成 | dc1d593 |
| 13 | 测试时耗治理（conftest 全局清 key + monkeypatch _make_llm_call + pytest-timeout 兜底，根治真实 LLM 卡顿/挂死） | ✅ 完成 | e7f84fb |
| 14 | 离线自检中枢（无 LLM 确定性健康探针：四引擎+适配层+记忆+静态资产，接入总控台实时可视化） | ✅ 完成 | 6f5d7f5 |
| 15 | 自动化调度中枢（Automation Hub：四引擎定时/周期自主运行 + 后台调度线程 + 持久化 + 总控台调度卡） | ✅ 完成 | 5b33160 |
| 16 | 实时活动总线（Event Bus：四引擎/调度/自检运行事件统一汇流 + /api/events 增量拉取 + 活动流页面实时可视化） | ✅ 完成 | 54462da |
| 17 | 事件持久化回溯与关键操作审计链（落盘 JSONL + 启动回放 + audit 标记 + /api/audit 增量审计 + 审计页实时可视化 + selfcheck 探针） | ✅ 完成 | c56d1e0 |
| 18 | 自主进化闭环（Self-Healing：离线自检失败+审计失败事件聚合→规则库转结构化补丁提议+健康分+导出待审，收口「可自愈/可自主进化」智能体 OS 信条） | ✅ 完成 | 1ac4a48 |
| 19 | 补丁预案升级（自愈提议结构化可执行预案 patch_plan + 可读 Markdown 报告导出 /api/heal/export-md + heal.html 预案展示/导出报告） | ✅ 完成 | （本提交） |
| 20 | 自愈闭环 2.0（补丁生成+沙箱验证门+人工合并门：proposal→可应用 diff 候选(规则/LLM降级骨架)→沙箱结构校验→confirm 才落地，绝不动源码；新增 /api/heal/generate|verify|patches|apply + heal.html 补丁仓库与合并弹窗） | ✅ 完成 | （本提交） |
| 21 | 多模态基座（统一 MediaAsset 抽象 + 可检索 AssetLibrary(SQLite) + 统一 generate() 登记入库 + GET/POST/DELETE /api/multimodal 资产库画廊 + multimodal.html 画廊UI；creation_domains 音频/图像/视频域标记 ready） | ✅ 完成 | （本提交） |
| 22 | 音频域深化（本地配乐合成：零依赖 struct 手写 WAV 真实可播放音频 I-V-vi-IV 和弦+鼓点；语音合成 voice/rate/pitch 透传 edge_tts；语音克隆占位降级；multimodal.html 音频模式选择+画廊内联 <audio> 播放器+时长徽章；注：本环境 wave.py 标准库损坏，已用手写 WAV 绕开） | ✅ 完成 | f670f03 |
| 23 | 图像域深化（图片域三模式：gen 文生图(本地 Pillow 真实信息图/有 key 走远程 API) / inpaint 局部重绘 / upscale 超分放大 LANCZOS 2x；均真实 PNG(real=true)，无 key 不崩；multimodal.html 图像模式卡+参考图路径；服务端 image 域模式校验） | ✅ 完成 | （本提交） |
| 24 | 视频域深化（视频域三模式：gen 文生视频(LLM 分镜→GIF 动图/有 key 走远程 MP4) / img2video 图生视频(Ken Burns 运动镜头) / clips 剪辑配音合成(图片序列幻灯片)；均真实 GIF/MP4(real=true)；multimodal.html 视频模式卡+参考图路径；服务端 video 域模式校验） | ✅ 完成 | （本提交） |
| 25 | 工作伙伴多智能体联邦（federation.py：伙伴注册/目标路由(关键词+显式 hint)/并行派发(ThreadPoolExecutor)/汇聚(去重+冲突检测)/审计链路；4 伙伴 code/creation/research/ops 复用 creation_domains 派发；服务端 /api/federation + /api/federation/dispatch + /federation 页；selfcheck 联邦探针 10→11） | ✅ 完成 | （本提交） |
| 26 | 长期记忆图谱（memory_graph.py：Entity/Relation/Fact 三件套 + SQLite 图谱；absorb 规则抽取 决策/约定/故障/接口/项目 类实体 + 关系三元组 + 事实落盘，隐私脱敏不存 key 值；recall 图遍历召回(实体+1跳关系+recap 注入)；decay 置信度衰减(trusted 豁免)；联邦派发自动沉淀；服务端 /api/memory-graph + absorb + recall + /memory-graph 页；selfcheck 记忆图谱探针 11→12） | ✅ 完成 | （本提交） |
| 27 | 超级 AGENT 内核（superagent.py：收口「目标理解→域路由→并行编排→收敛(三级护栏)→自检质量门→记忆沉淀」统一闭环，复用 federation+memory_graph+selfcheck+event_bus；LLM 抽取意图/域/约束，失败回退联邦路由 + 注入历史经验；无 LLM 全程规则兜底；单目标跨 2+ 域编排成功；服务端 /superagent + /api/superagent + /api/superagent/run + superagent.html；selfcheck 超级AGENT 探针 12→13） | ✅ 完成 | （本提交） |
| 28 | 执行落地（superagent.py 执行器阶段 + 可插拔执行器注册中心 register_executor）：把并行编排的伙伴方案真正落地为真实产物（默认执行器把方案写成 outputs/superagent/<ts>_<域>.md 交付文件，可热插拔真实执行器 code→autonomous / creation→multimodal 等）；执行阶段进 trace + 审计链；产物路径回写 result.executions/artifacts 与记忆沉淀；superagent.html 新增「执行落地」卡片；selfcheck 探针加执行落地断言，仍 13 探针全绿） | ✅ 完成 | （本提交） |
| 29 | 真实执行器热插拔（superagent.py 模块级 `_exec_code/_exec_research/_exec_ops/_exec_creation` 默认注册进 EXECUTORS，覆盖 Phase28 的默认 .md 兜底）：code 有 LLM 让 LLM 产出真代码并 `compile` 校验、无 LLM 产出可编译骨架；creation 产出可回读 `asset_manifest.json`；research 开启 `LMW_SA_ALLOW_FETCH=1` 真抓取落盘、否则落地研究简报；ops 生成可 `bash -n` 校验的 `deploy.sh`；均异常隔离 + `:memory:` 临时目录兜底；superagent.html 执行卡片按类型展示 + 可执行动作提示；selfcheck 探针加「真实产物 >=1」断言，仍 13 探针全绿） | ✅ 完成 | （本提交） |
| 30 | 自主编码执行器（`_exec_code` 升级为「生成→编译→冒烟运行自验证→失败有限自修复[LLM]→落 run.log」闭环）：子进程冒烟运行生成代码（15s 超时 + PYTHONIOENCODING=utf-8 防 cp936 乱码/崩溃），运行日志落为独立产物 `.run.log`；LLM 在场时冒烟失败自动把报错喂回 LLM 修正重跑（最多 2 次，`healed` 标记）；无 LLM 骨架从「可编译但 raise NotImplementedError」升级为**可运行骨架**（main 打印就绪并 return 0，冒烟通过）；返回体新增 `run_ok/healed` 字段；superagent.html 执行卡片加「▶ 冒烟通过·自修复 / ▶ 冒烟失败」徽标；测试扩至 22 例（冒烟日志/可运行骨架/自修复/无 LLM 失败如实上报） | ✅ 完成 | （本提交） |
| 31 | 真实创作执行器（creation→multimodal 真产出：新增 `_exec_creation_real` + `_creation_subdomain`，经既有 `multimodal_adapters.render` 真实渲染媒体文件——PNG 信息图 / MP3 音频 / GIF 动图，无 LLM 自动确定性模板兜底全程可用；子域解析优先伙伴蓝图标注、无标注按目标关键词路由（视频→video/音频→audio/默认 image）；manifest 升级含 `render` 结果（file/mime/real/note/meta）；适配层不可用/失败回退纯清单不崩，旧 `_exec_creation`（纯清单）保留可回切；superagent.html creation 产物类型与动作提示更新；测试扩至 25 例） | ✅ 完成 | （本提交） |
| 32 | 插件中枢（`plugin_hub.py`：Connector 连接器注册 + 可用性降级 / Expert 专家画像 / 目录扫描发现 / 自动接入超级 AGENT 路由）：`Connector(name, category, description, call_fn, env_required, optional_dep)` 声明能力, `check()` 自动检测 env 缺失降级 unavailable; `Expert(name, domain, system_prompt, description, tags)` 提供 LLM 专家画像; `PluginHub` 单例提供 register/list/discover/wire; `_bootstrap()` 默认注入 3 连接器(health/recall/recent_runs) + 3 专家画像(codereview/design_lead/ops_sre); `superagent._wire_plugins()` 把可用专家域并入 understand.domains(上限 4, 不覆盖 LLM 抽取结果), 接入摘要进 trace + result["plugins"]; 新增 `/plugins` 前端 + 8 个 `/api/plugins*` 端点(列出/注册/discover/wire/调用连接器); selfcheck 探针 13→14 全绿） | ✅ 完成 | （本提交） |
- **Phase 21 关键落地（多模态基座 · Unified Media Foundation）**：
    - `MediaAsset` dataclass：音频/图像/视频共享统一模型（id/kind/path/mime/source/real/meta/note/created_at/session_id）。
    - `AssetLibrary`：本机 SQLite 元数据索引 `<cwd>/.lmw_media/assets.db` + 目录落盘 `outputs/multimodal`；`save/list/get/delete/quota_bytes`（检索·去重·配额清理）；`list()` 返回含 `url=/outputs/<basename>` 便于前端直链。
    - `register_asset(art,...)`：把 `multimodal_adapters.render` 的返回 dict 规范化为 `MediaAsset` 入库（补 `url`）。
    - `generate(domain,brief,...,base_dir,llm_call)`：包装既有 `render`（真实渲染 + 降级链，主流程永不崩）+ 登记资产库；不支持域返回 None。
  - `server.py`：新增 `GET /api/multimodal`（`?kind=&q=&limit=` 检索画廊）、`POST /api/multimodal/generate`（统一生成入口 + 登记）、`DELETE /api/multimodal/<id>`（删资产 + 文件）；保留 `/api/multimodal/render` 兼容。
  - `creation_domains.py`：音频/图像/视频三域 `adapter` 由 `mcp`→`native`、`status` 由 `pending_adapter`→`ready`（适配已落地，与现状对齐）。
  - `multimodal.html`（Nebula 暗色重构）：保留生成卡（音频/图像/视频三域 + 蓝图 + brief），新增「📚 资产库画廊」（网格缩略图 + 类型徽章 + 真实/降级标记 + 搜索 + 刷新 + 删除），生成后自动刷新；并清理旧「灵梦磐石」残留文案。
  - 测试：`tests/test_multimodal.py` **7 例全过**（AssetLibrary CRUD/检索/删除 · register_asset · 三域降级登记 · 非法域返 None · quota 清理 · 服务端 GET/POST/DELETE 全链路 + /multimodal 页面含画廊）；所有写盘用例均 `chdir(tmp_path)` 隔离，规避运行时污染；全量 pytest 回归 **522 passed / ~75s**（Phase 21 净增 7 例）。
  - **未上线(需重打包)**：multimodal.py + server.py `/api/multimodal*` + multimodal.html + creation_domains 改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

- **Phase 22 关键落地（音频域深化 · Audio Domination）**：
  - `multimodal_adapters.py` 音频域 `_render_audio` 扩展 `mode` 分发：`tts`(默认·语音合成) / `music`(本地配乐) / `clone`(语音克隆占位)：
    - `tts`：edge_tts 真实 MP3，`voice/rate/pitch` 透传（如 `zh-CN-YunxiNeural` / `+10%` / `+5Hz`）；无 edge_tts/网络时降级「文字稿+声波占位图」。
    - `music`：新增 `_render_music`，**零依赖 struct 手写 WAV**（绕开本环境损坏的 wave.py 标准库），合成 I-V-vi-IV 和弦进行 + 低频鼓点，输出真实可播放 `audio/wav`（`bpm`/`小节` 可从 brief 解析）；无 key/无网络即真实音频产能。
    - `clone`：语音克隆占位（需 reference_audio + 克隆 API），无 key 降级「声波占位图 + 结构说明」。
  - `multimodal.generate` + `server.py _multimodal_generate` 透传 `mode/voice/rate/pitch`；`music/clone` 模式约束仅 audio 域（误用 image/video 返回 400）。
  - `multimodal.html` 加「音频模式」选择卡（语音合成/本地配乐/语音克隆）+ voice/rate/pitch 输入；画廊音频卡加内联 `<audio controls>` 播放器 + 时长/voice 徽章。
  - 测试：`tests/test_multimodal.py` **扩至 11 例**（新增 本地配乐真实 WAV / 语音克隆降级 / TTS 模式透传 / 服务端 music 模式）；全量 pytest 回归 **526 passed / ~73s**（Phase 22 净增 4 例）。
  - **环境坑（重要）**：本机 Python `wave` 标准库 `wave.py` 被 NUL 字节污染，`import wave` 在任何文件均报 `SyntaxError: source code string cannot null bytes`（与项目代码无关）；已用手写 WAV 二进制（struct + 44 字节 RIFF 头）完全绕开，音频产能不受影响。
  - **未上线(需重打包)**：multimodal_adapters.py + multimodal.py + server.py + multimodal.html 音频深化改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

- **Phase 23 关键落地（图像域深化 · Image Domination）**：
  - `multimodal_adapters.py` 图片域 `_render_image` 扩展 `mode` 分发：`gen`(文生图) / `inpaint`(局部重绘) / `upscale`(超分放大)：
    - `gen`：有图像生成 key（`LMW_IMAGE_API_KEY` / `OPENAI_API_KEY`）走 OpenAI 兼容文生图 API 真实生成（`response_format=b64_json`，落真实 PNG，`meta.gen="remote"`）；无 key / 失败自动回退本地 Pillow 真实信息图（`meta.gen="local"`，LLM 设计驱动/模板回退），**始终真实 PNG**。
    - `inpaint`：新增 `_render_image_inpaint` —— 给定可选参考图(路径)在中心区域重绘（渐变填充 + 标注），无参考图用演示画布，产出真实 PNG（`meta.inpaint=True`）。
    - `upscale`：新增 `_render_image_upscale` —— 给定可选参考图 LANCZOS 2x 放大（`meta.scale=2`，`width/height` 真实放大），无参考图用演示样本，产出真实 PNG（`meta.upscale=True`）。
    - `render()` 新增 `image_path` 形参，image 域特判透传 `mode/image_path`（与 audio 域 voice/rate/pitch 并列）。
  - `multimodal.generate` 透传 `image_path`；`server.py _multimodal_generate` 解析 `image_path` 并校验 `inpaint/upscale` 仅 image 域（误用 audio 返回 400）。
  - `multimodal.html` 加「图像模式」选择卡（文生图/局部重绘/超分放大）+ 参考图路径输入；域切换时 audio/image 选项卡互斥显隐；genBtn 在 image 域时 body 加 `mode` + 可选 `image_path`。
  - 测试：`tests/test_multimodal.py` **扩至 15 例**（新增 文生图本地真实 PNG / 局部重绘真实 PNG / 超分放大尺寸 2x / 服务端 inpaint·upscale 模式路由 + 误用 400）；`tests/test_multimodal.py` 单文件 **15 passed**；全量 pytest 本环境 `527 passed / 3 failed`，**3 例失败均为全量套件顺序/临时目录权限干扰（test_audio_fallback_structure / test_round3::test_task_persist / test_server_review_report_e2e），三者单独运行均通过，且与 Phase 23 图像代码无关（环境 safe-delete 垫片干扰 pytest 临时目录清理，报 `Permission denied` / `rm_rf error`），非本次改动回归**。
  - **未上线(需重打包)**：multimodal_adapters.py + multimodal.py + server.py + multimodal.html 图像深化改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。看最新页用「启动源码面板.bat」(源码 8320)。

**本会话新增能力**：`decompose_engine.py`（层级分解）、`creation_domains.py`（四域注册/路由）、`autonomous.py`（自主闭环）、`goal_pipeline.py`（全链路流水线）、`multimodal_adapters.py`（真实多模态适配层）；
页面 `orchestrate.html`/`studio.html`/`autonomous.html`/`pipeline.html`/`multimodal.html`；API `/api/decompose`、`/api/creation/domains`、`/api/creation/dispatch`、`/api/autonomous`、`/api/pipeline`、`/api/multimodal/render`、`/outputs/<name>`（真实媒体预览）；
导航与命令面板已集成「编排中枢 / 创作工作台 / 自主模式 / 目标流水线 / 多模态实验室」。

**质量门（已通过）**：Python `py_compile` 全绿；`node --check` 全绿；源码服务 curl 烟测——五页面 + 四域分发 + 自主循环 + 目标流水线 + 多模态三域真实产出全链路 200（图片 PNG / 音频 edge_tts 真 MP3 / 视频 GIF 均 `real=true`，中文文件名预览 200）；全量 pytest 回归 `426 passed`（系统 Python 3.11）。

**Phase 8 关键落地**：
- 图片 = Pillow 真实 PNG 信息图（零 key）；音频 = edge_tts 真实 MP3（已验证合成），不可用时降级文字稿+声波占位；视频 = ffmpeg 缺失下 Pillow 真实 GIF 动图交付。
- 流水线 Stage4 已接入适配层，创作域步骤自动落真实文件并回写 `artifact.url`；pipeline.html 执行卡片渲染真实媒体预览。
- 修复：中文文件名预览端点需 `unquote` 解码（原 `%xx` 直拼路径 404）。

**遗留待用户决策**：冻结 exe 重打包（PyInstaller）仅在你明确许可后执行；当前 8318 面板运行的 exe 不含本轮后端改动，已通过源码服务验证，标注「需重打包才上线」。

---

## 九、下一阶段展望（Phase 9+，待你定向）

- **真实文生图 / 文生视频升级**：当前 image 为 Pillow 信息图、video 为 GIF 动图；接入文生图/文生视频 API 或 MCP（配置 key 后）可升级为「真实生成式图像/MP4」，并把 ffmpeg 合入视频管线。
- **流水线×自主深度融合**：在「执行」阶段对编程域调用 `autonomous` 真执行（代码生成+单测+评审+PR），对创作域派发到适配层，形成「蓝图→真实交付」闭环。
- **跨会话记忆喂入流水线**：把语义记忆（Phase 2）作为 decompose/dispatch 的系统上下文，使计划具备历史偏好与决策一致性。
- **可观测总控台**：把编排/创作/自主/流水线四类轨迹统一进一个实时大屏（复用 observability 统计/成本/计划三看板）。

**Phase 10 关键落地**：
- 新增 `GET /api/engines` 聚合快照：四引擎（编排中枢 / 创作域 / 自主模式 / 全链路流水线）状态卡 + 编排运行态 + 创作域清单 + 语义记忆条目 + 总控台轨迹（按引擎计数 + 最近 20 条）+ LLM 后端。
- 新增 `POST /api/engines/run` 统一启动器：把总控台「一键启动」路由到 `decompose` / `creation` / `autonomous` / `pipeline` 四引擎真实入口，运行后写入 `_ENGINE_RUNS` 环形缓冲（上限 200，模块级全局，进程内跨请求共享），供总控台实时轨迹轮询。
- 新增 `control_center.html`（Nebula 暗色单页）：KPI 条 + 四引擎卡片 + 统一启动器表单 + 实时轨迹时间线；每 4s 轮询 `/api/engines`。导航与命令面板已集成「统一总控台」。
- 测试：`tests/test_engines_api.py` 7 例（状态快照结构 / 四引擎启动 / 缺参 400 / 未知引擎 400 / 创作域缺 domain 400）；全量 pytest 回归全绿（系统 Python 3.11）；源码服务 HTTP 烟测 `/api/engines`(200) + `/api/engines/run`(decompose 真实 9 步) + `/control-center`(200) 全链路可达。
- 注：`/api/engines/run` 仅接受 POST；真实 LLM 后端下 pipeline 多阶段调用较慢（非缺陷，逻辑已被规则兜底单测覆盖）。

**Phase 11 关键落地（流水线×自主深度融合）**：
- `goal_pipeline.py`：新增 `_extract_code_blocks`（围栏代码块抽取，含语言→扩展名映射）、`_slugify`、`_autonomous_execute`（跑 `autonomous.run` 自驱回路 → 抽取代码落盘至 `outputs/code/<slug>/` → `.py` 经 `py_compile` 编译校验作为评审门 → 回传 `{reached,iterations,files,review}`）。
- `run_pipeline` 增 `do_autonomous=True` / `max_autonomous_iter=4`：`Stage4` 对 `code`(`any`→code) 域步骤**优先驱动自主回路**；若抽得代码则 `plan` 取自主结论 + 执行项挂 `autonomous` 字段；无代码则**自动回退** `creation_domains.dispatch` 蓝图（不破坏旧行为）。`do_autonomous=False` 时执行项 `autonomous=None`。
- `_assemble_delivery` 交付稿对自主步骤追加「🤖 自主执行 N 轮 · 达成=… · 生成文件 · 编译校验 X/Y 通过 + 代码目录」。
- `server.py` `_engines_run` 对 pipeline 透传 `do_autonomous` / `max_autonomous_iter`（默认开）。
- `pipeline.html`：执行卡对含 `autonomous` 的步骤渲染「🤖 自主回路」卡（达成/未达成徽章 + 轮次 + 编译校验 X/Y + 生成文件列表 + 未通过告警 + 目录跳转），含 `.au-card` 样式。
- 测试：`tests/test_pipeline_autonomous.py` **6 例全过**（抽代码 / 真实 LLM 仿真写盘+编译通过 / 坏代码编译失败 / 无 LLM 退化为空轨迹 / 流水线 code 域挂 autonomous 字段且回退蓝图 / do_autonomous=False 无字段）；`py_compile` 全绿；源码服务 HTTP 烟测最小流水线（`max_dispatch=1` + 关渲染 + 关自主）6 阶段 + 1 执行项 + `autonomous=None` 全链路 200。
- 设计约束：自主引擎为纯 LLM 规划回路（不执行 shell），融合仅做「抽代码落盘 + 编译校验」，安全可演示；真实 LLM 下 pipeline 多阶段较慢（非缺陷）。
- **未上线(需重打包)**：goal_pipeline.py + server.py 后端 + pipeline.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

**Phase 12 关键落地（多模态生成中枢 2.0 · LLM 智能设计驱动）**：
- `multimodal_adapters.py`：新增 `_parse_json_block`（兼容 ```json 围栏 + 前后杂文本的稳健 JSON 抽取）、`_design_image`（LLM 设计信息图布局 JSON：标题/副标题/要点/配色/风格）、`_design_audio_script`（LLM 提炼朗读文稿）、`_design_video_shots`（LLM 规划分镜 JSON：标题 + 多画面字幕）；`render` 增 `llm_call` 参数；三域渲染先调 LLM 设计再真实产出，LLM 失败/缺 key 自动回退确定性模板（配色板 nebula/sunset/ocean/forest/rose 映射）。
- `goal_pipeline.py` `Stage4`：对创作域 `ma.render(..., llm_call=llm_call)` —— 流水线内创作步骤自动获得 LLM 智能设计。
- `server.py` `_multimodal_render`：注入 `llm_call=self._make_llm_call()`（无 key 返回 None → 模板回退，实验室页默认即获升级）。
- 测试：`tests/test_multimodal_adapters.py` **10 例全过**（设计生效/容错回退/降级占位/未知域/解析器）；`py_compile` 全绿；HTTP 烟测真实 LLM 设计路径端到端可用（image `llm_designed=True` 真实 PNG 落盘）。
- 设计约束：不引入新依赖/新 key，复用已配置 SenseNova；单域/单步 LLM 失败不影响其余；真实 LLM 下多模态设计较慢（非缺陷）。
- **未上线(需重打包)**：multimodal_adapters.py + server.py + goal_pipeline.py 后端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

**Phase 13 关键落地（测试时耗治理）**：
- `conftest.py` 新增 autouse fixture `_no_real_llm`：① `monkeypatch.delenv` 清除 8 类真实 LLM key 环境变量；② `monkeypatch.setattr(Handler, "_make_llm_call", lambda self,*a,**k: None)` 强制所有 web server 测试入口返回 None（进程内线程 server + 子类 `_StubHandler` 均沿 MRO 命中），根治测试内真实 server 经 `_load_dotenv` 注入 `.env` key 误触发真实商汤 API 的卡顿/挂死。
- `pytest.ini`：`[pytest] timeout = 120` 全局超时兜底；装 `pytest-timeout` 2.4.0（系统 Python 3.11，`env -u` 绕过 safe-delete）。
- 验证：带原生 key 全量回归 **380 passed / 0 failed / 48.98s**（对比此前 9m+ 卡死 / 去 key 8m40s，提速约 10 倍）；e2e server 测试单测 7s 通过。

**Phase 14 关键落地（离线自检中枢 · Self-Check Hub）**：
- 新增 `lingmengwork/selfcheck.py`：无 LLM 依赖、确定性、可重复的健康探针。8 项检查覆盖——①核心模块导入 ②任务拆解(规则兜底) ③创作域(规则兜底) ④自主模式(无 LLM 闭环) ⑤全链路流水线(无 LLM) ⑥多模态适配层(模板回退) ⑦跨会话记忆(捕获+召回) ⑧关键静态资产(6 个前端页)。`run()` 返回 `{ok, score(0-100), passed, total, all_ok, checks[], ts}`；`main()` 提供 CLI（`python -m lingmengwork.selfcheck`，exit 0/1）。
- `server.py` 新增 `GET /api/selfcheck`（路由 `from .. import selfcheck` 调 `selfcheck.run()`），接入统一可观测。
- `control_center.html` 新增「系统离线自检」卡：按钮 + 状态条 + 报告渲染，载入时自动跑一次（健康分 + 逐检查项明细）。
- 测试：`tests/test_selfcheck.py` **10 例全过**（报告结构/各引擎探针/静态资产/CLI exit 0）；`py_compile` 全绿；HTTP 烟测 `/api/selfcheck` 返回 8/8(100 分) + `/control-center` 含 `btnSelf`。
- 设计约束：纯离线、无外部 key、不触发任何网络；单引擎失败只降分不影响其余；作 CI/启动健康门固定入口。
- **未上线(需重打包)**：selfcheck.py + server.py `/api/selfcheck` + control_center.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

**Phase 15 关键落地（自动化调度中枢 · Automation Hub）**：
- 新增 `lingmengwork/automation_hub.py`：纯标准库 `AutomationHub`（`tasks` 增删改查 + JSON 持久化 + `tick` 调度判定 + `run_task` 规则兜底执行四引擎）+ `SchedulerThread`（后台 `threading` 守护线程，每 5s 扫一次到期任务）+ `cron/daily/interval` 解析 + 全局单例 `get_hub()` 懒启动（首次 API 访问自动拉起调度线程，进程内跨请求共享）。
- `server.py`：新增 `GET /automation`（页面）、`GET /api/automations`（列表）；`POST /api/automations`（新增：name/kind/payload/schedule/enabled）、`POST /api/automations/<id>/run`（立即运行）、`POST /api/automations/<id>/toggle`（启停）、`POST /api/automations/<id>/delete`（删除）；方法内 lazy import `automation_hub` 并 `get_hub().start()`。
- `automation.html`（Nebula 暗色单页）：任务列表（状态/下次运行/上次结果）+ 新增表单（四种 kind：decompose/creation/autonomous/pipeline）+ 调度表达式帮助 + 实时轮询 4s + 立即运行/删除/启停按钮。导航与命令面板已集成「自动化调度」。
- `selfcheck.py`：静态资产清单加 `web/static/automation.html`，导入探针加 `automation_hub`；`.gitignore` 加 `.lmw_automation.json`（调度持久化副产物）。
- 测试：`tests/test_automation_hub.py` **24 例全过**（cron 解析/interval 触发/各 kind 规则兜底执行/持久化/并发安全/服务端 API 全链路）；全量 pytest 回归 **483 passed / 14s**；HTTP 烟测页面 200 + 调度线程运行 + 登记/运行/启停/删除全绿。
- 设计约束：零三方依赖、无外部 key、不触发网络；四引擎执行均走规则兜底（`llm_call=None`），真实 LLM 任务仅登记/调度，执行时仍走既有引擎入口；调度线程为 daemon，随主进程退出。
- **未上线(需重打包)**：automation_hub.py + server.py `/api/automations*` + automation.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

**Phase 16 关键落地（实时活动总线 · Live Activity Bus）**：
- 新增 `lingmengwork/event_bus.py`：纯标准库 `EventBus`（线程安全 RLock 环形缓冲 + 自增 id 游标 + `recent(limit,since_id)` 增量拉取 + `counts_by_source` + 全局单例 `get_bus()`）；`emit(source,kind,msg,data)` 异常静默，可被 web 请求线程与调度守护线程并发读写。
- `server.py`：新增 `GET /api/events`（增量事件拉取 JSON：`{ok,events,total,counts}`，支持 `?since=<id>&limit=<n>`）+ `GET /activity`（页面）；`GET /api/selfcheck` 跑完 emit `selfcheck` 事件；`_engines_run` 成功/失败 emit `engine` 事件；`_automations_create/run/delete/toggle` 各动作 emit `automation` 事件。新增 `_emit`/`_events_get` 两个方法，各接入点懒导入 `event_bus`。
- `activity.html`（Nebula 暗色单页）：实时事件流（4s 轮询 `/api/events` 增量 + 源色标徽章 + 本地时间 + 消息 + 可折叠 data JSON + 累计/分源统计 + 清空视图）；导航与命令面板已集成「实时活动」。
- `selfcheck.py`：导入探针加 `event_bus`（8 模块）、静态资产清单加 `web/static/activity.html`（现 8 个齐备）。
- 测试：`tests/test_event_bus.py` **11 例全过**（emit 结构/增量游标/环形裁剪/分源计数/并发安全/服务端 API/引擎动作 emit 接入）；全量 pytest 回归 **494 passed**；HTTP 烟测页面 200(真实活动页非 fallback) + 自检 8/8 + 事件汇入总线（selfcheck/automation 双源）。
- 设计约束：零三方依赖、无外部 key、不触发网络；事件丢失不影响主流程；与 Phase 10/14/15 可观测点收口为统一总线。
- **未上线(需重打包)**：event_bus.py + server.py `/api/events` + activity.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

**Phase 17 关键落地（事件持久化回溯与关键操作审计链 · Audit Trail）**：
- `event_bus.py` 扩展：新增 `persist_path` 落盘 JSONL（每条事件行追加写盘，全量跨重启保留）+ 启动回放（`__init__` 读盘回填内存环形缓冲并接续自增 id 游标）+ `audit` 标记字段 + `audit_trail(limit, since_id, source)` 合并内存+磁盘去重筛选审计事件 + `init_bus(persist_path)` 显式初始化单例。
- `server.py`：启动入口 `run_web` 注入 `init_bus(persist_path=<cwd>/.lmw_events/events.jsonl)`；`_emit` 增 `audit` 参数，关键操作（引擎 run/run_fail、自动化 create/run/run_fail/delete/toggle）统一标记 `audit=True`；新增 `GET /api/audit`（增量审计回溯 + 源筛选）+ `GET /audit` 审计页；`/api/events` 透传 `audit` 字段。
- `audit.html`（Nebula 暗色）：KPI 条（审计总数/引擎/自动化/其他）+ 来源筛选 + 4s 轮询 `/api/audit` + 审计链表（时间/来源徽章/动作/消息/数据查看）；导航与命令面板已集成「操作审计」。
- `selfcheck.py`：静态资产加 `web/static/audit.html`；新增「活动总线(事件+审计链)」探针（发射审计事件 + 审计链回溯），现 9 个探针全绿。
- 测试：`tests/test_audit.py` **6 例全过**（持久化回放/审计筛选/源筛选/并发写盘安全/服务端 /api/audit / selfcheck 探针）；`test_selfcheck.py` 探针数断言 8→9；全量 pytest 回归 **500 passed**；HTTP 烟测 `/audit` 真实页面 + `/api/audit` 跨重启含审计事件 + `/api/events` 含 audit 字段全绿。
- 修复：Phase 15 遗留 `_automations_create` 中 `self._emit(...)` 被错误置于 `except` 块的 `return` 之后（死代码），导致创建任务不发射事件；已移至 `except` 外、return 之前。
- **未上线(需重打包)**：event_bus.py(持久化) + server.py `/api/audit` + audit.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

**Phase 18 关键落地（自主进化闭环 · Self-Healing Patch Proposer）**：
- `self_heal.py`（新增，纯标准库）：`collect_signals()` 聚合离线自检(selfcheck)失败项 + event_bus 审计失败事件(run_fail/error/fail)；内置规则库 `_RULES`（selfcheck_fail / engine_run_fail / automation_run_fail / generic_fail），把每条信号转结构化 `Proposal`(severity / area / symptom / hypothesis / actions / confidence / auto_fixable / source_ref)；`propose()` 汇总健康分(100−严重度扣分) + 信号量 + 提议数 + 各严重度计数；`export_proposals()` 落盘 `.lmw_heal/proposals.jsonl`（human-in-the-loop 待审，**不直接改源码**，可人工复核或后续 LLM 消费）；`run()` 端到端采集当前进程信号。
- `server.py`：新增 `GET /api/heal`（聚合信号生成补丁提议）、`POST /api/heal/export`（落盘待审区）、`GET /heal` 页面。
- `heal.html`（Nebula 暗色）：KPI 条（健康分/信号量/补丁提议/可自动修复）+ 严重度分布 + 提议卡（severity 色标徽章 + 区域 + 症状 + 假设 + 建议动作清单 + 置信度 + 可否自动修复）+ 4s 轮询 `/api/heal` + 导出待审按钮；导航与命令面板已集成「自主进化」。
- `selfcheck.py`：静态资产加 `web/static/heal.html`；新增「自主进化(自愈提议)」探针（无信号健康分 100 + 注入失败信号生成精确区域提议），现 10 个探针全绿。
- 测试：`tests/test_self_heal.py` **8 例全过**（无信号健康分 / selfcheck 失败生成 / 静态缺失精确诊断 / 引擎失败事件 / 自动化失败事件 / 严重度扣分 / 导出落盘 / 服务端 API）；`test_selfcheck.py` 探针数断言 9→10；全量 pytest 回归 **508 passed / ~16s**；HTTP 烟测「运行失败→/api/heal 健康分下降 + 命中『引擎:pipeline』+ 导出落盘 .lmw_heal」闭环全绿。
- 收口「工程信条」：本地优先 + 零依赖内核 + 可审计 + 可自愈 + 可自主进化 —— Phase 14(自检) / 16(活动总线) / 17(审计链) / 18(自愈提议) 组成完整「神经系统 + 自愈闭环」。

**Phase 19 关键落地（补丁预案升级 · Executable Patch Plan + 可读报告）**：
- `self_heal.py` 升级：① `Proposal` 新增 `patch_plan` 字段（`{title, steps[], verify, risk}` 结构化可执行修复预案）；② `_build_selfcheck/engine_fail/automation_fail/generic_fail` 四条规则各填充精准预案——自检类给 `git checkout` 恢复命令或资产恢复步骤 + 重启面板 + 复跑自检；引擎类给 /api/audit 定位 + key/网络核对 + 规则兜底手动验证 + 补回归；自动化类给 `hub.run_now` 复现 + 调参/下线；generic 给定位 + 观察/提 Issue。③ `export_proposals()` 升级同时生成 `proposals_<ts>.md` 可读报告（含完整补丁预案 + 严重度 + 来源 + 验证 + 风险）；新增 `_render_markdown()`；返回含 `md_path`。仍 **human-in-the-loop**：只生成建议与可执行 Step，不改源码。
- `server.py`：新增 `POST /api/heal/export-md` 路由 + `_heal_export_md`（调 `self_heal.run()` 取当前报告 → `export_proposals` 落盘 → 返回 `md_path`）。
- `heal.html`（Nebula 暗色）：提议卡加「🛠 可执行补丁预案」展开区（步骤有序列表 + ✓验证 + ⚠风险）；新增「📄 导出可读报告」按钮（调 `/api/heal/export-md`，toast 返回路径）；新增 `.plan` 样式组。
- 测试：`tests/test_self_heal.py` 扩至 **12 例**（新增 patch_plan 结构断言 / 静态缺失精准命令 / 可读 md 导出 / 服务端 export-md 端点 + /heal 页面含导出按钮）；全量 pytest 回归 **512 passed / ~19s**（Phase 19 净增 4 例）；HTTP 烟测「注入失败事件 → /api/heal/export-md 落盘 md 含补丁预案 + creation 区域」闭环全绿。
- 设计约束：零三方依赖、无外部 key、不触发网络；补丁预案仍人工复核门，系统不自动改源码；报告与 jsonl 双产物并存（机器读 + 人读）。
- **未上线(需重打包)**：self_heal.py + server.py `/api/heal/export-md` + heal.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。
- **未上线(需重打包)**：self_heal.py + server.py `/api/heal` + heal.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

**Phase 20 关键落地（自愈闭环 2.0 · 补丁生成 + 沙箱验证门 + 人工合并门）**：
- `self_heal.py` 升级：① 新增 `Patch` 类（补丁候选元信息）；② `generate_patch(proposal, repo_root, llm_call)` —— LLM 可用且 target 清晰时调 `llm_call` 生成真实代码 diff，否则走**规则降级**生成「注释型骨架 diff」（含症状/假设/结构化步骤占位，**绝不编造源码**）；落盘 `.lmw_heal/patches/PATCH_<id>_<ts>.diff` + 同名 `.json` 元信息。③ `sandbox_verify(patch_id, repo_root)` —— 临时沙箱校验补丁结构合法性（头/步骤齐全）+ 若 target 为具体文件则 `py_compile` 校验（不改它），不碰真实 repo。④ `apply_patch(patch_id, repo_root, confirm)` —— **人工合并门**：`confirm=False` 仅提示需确认；`confirm=True` 才落地，先写 `.lmw_heal/backups/<ts>/<patch>.confirmed.json` 确认态 + 备份目标文件，**仍绝不自动改源码**。⑤ `list_patches(repo_root)` 列出全部补丁（含 diff 文本供前端展示）。
- `server.py`：新增 `GET /api/heal/patches`（列表）+ `POST /api/heal/generate`（按 proposal_id 重新 propose 定位→生成）+ `POST /api/heal/verify`（沙箱校验）+ `POST /api/heal/apply`（人工合并门）；对应 `_heal_patches/_heal_generate/_heal_verify/_heal_apply` 四个 Handler 方法。
- `heal.html`（Nebula 暗色）：① 顶部新增「📦 补丁仓库 (Phase 20 · 人工合并门)」区，列出已生成补丁（patch_id/生成方式/目标/📋diff/✓验证/⚙合并）；② 提议卡加「🛠 生成补丁」「⚙ 人工合并」按钮；③ 新增「⚙ 人工合并确认」弹窗（勾选确认框→确认合并）；④ 补 `.repobox/.patchrow/.patchdiff/.modal` 样式组；前端轮询补丁仓库（8s）。
- 测试：`tests/test_self_heal.py` 扩至 **15 例**（新增 生成→列表含diff→沙箱PASS→未确认不落地→确认记录状态+备份目录→缺失patch报错 / 验证缺失patch / 服务端 generate|verify|patches|apply 全链路 + /heal 含补丁仓库UI）；全量 pytest 回归 **515 passed / ~75s**（Phase 20 净增 3 例）；HTTP 烟测「注入 run_fail→/api/heal 取 proposal id→generate→verify PASS→patches 含 diff→apply(未确认)→apply(确认)→/heal 含补丁仓库」闭环全绿。
- 设计约束：零 LLM 亦工作（规则兜底），human-in-the-loop 终态，沙箱隔离，可端到端验证；补丁仅生成候选与确认态，**绝不自动改源码**；LLM 真实 diff 分支预留 `llm_call` 形参，待配 key 即启用。
- **未上线(需重打包)**：self_heal.py(Phase20) + server.py `/api/heal/{generate,verify,patches,apply}` + heal.html(补丁仓库/合并弹窗), 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

- **Phase 24 关键落地（视频域深化 · Video Domination）**：
  - `multimodal_adapters.py` 视频域 `_render_video` 升级为 `mode` 分发（与音频/图像域对齐）：`gen`(文生视频) / `img2video`(图生视频) / `clips`(剪辑配音合成)：
    - `gen`：有视频生成 key（`LMW_VIDEO_API_KEY`/`OPENAI_API_KEY`）走 OpenAI 兼容文生视频 API 真实生成 MP4（`meta.gen="remote"`）；无 key / 失败自动回退本地 LLM 分镜 → Pillow 真实 GIF 动图（`meta.gen="local"`，LLM 设计驱动/模板回退），**始终真实 GIF/MP4**。
    - `img2video`：给定参考图(路径)做 Ken Burns 运动镜头（缩放+平移）合成真实 GIF；无参考图用演示画布，产出真实 GIF（`meta.img2video=True`）。
    - `clips`：给定可选参考图序列(逗号分隔)合成图片幻灯片 GIF 动图；无图用演示画布（`meta.clips=True`）；配音封装需 ffmpeg/视频 key，本地降级静帧 GIF。
    - `render()` 视频域透传 `mode`/`image_path`；`generate()` 已透传。
  - `server.py`：`_multimodal_generate` 校验 `img2video/clips` 仅 video 域（误用返回 400）；video 域非法模式默认归 `gen`（避免默认 tts 被拒）；docstring 补 video 域说明。
  - `multimodal.html` 加「视频模式」选择卡（文生视频/图生视频/剪辑配音）+ 参考图路径输入（多图逗号分隔）；域切换显隐 + genBtn 透传 mode/image_path；sub 文案与视频 dom 卡片描述更新。
  - 测试：`tests/test_multimodal.py` **扩至 20 例**（新增 文生视频本地真实 GIF / 图生视频参考图 Ken Burns 真实 GIF / 剪辑配音多图幻灯片真实 GIF / 远程 MP4 降级分支(mock) / 服务端三模式路由 + 误用 400 + 默认归 gen + 页面含视频卡）；全量 pytest 回归 **535 passed / 37.9s**（0 failed）。
  - **未上线(需重打包)**：multimodal_adapters.py + server.py + multimodal.html 视频深化改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

- **Phase 25 关键落地（工作伙伴多智能体联邦 · Multi-Agent Federation）**：
  - `federation.py`（新增，纯标准库）：`Partner` 数据类（id/name/domain/tools/max_iter/emoji/theme/desc）+ `Federation`：
    - 默认注册 **4 伙伴**：`code`(编码) / `creation`(创作) / `research`(研究) / `ops`(运维)，各有专属工具集与主题色。
    - `route(goal, hint_domains)`：关键词映射（如「编码/函数/软件」→code、「视频/配图」→creation、「研究/竞品」→research、「部署/上线」→ops）自动路由到 1..N 伙伴；`hint_domains` 显式覆盖；无命中回退 code。
    - `dispatch(...)`：`ThreadPoolExecutor` **并行派发**各伙伴 `_run_partner` → 按路由顺序重排；单伙伴异常被 `try/except` 隔离（status=error，整体不崩）；每次派发/完成进 **事件总线 + 审计链**（`emit(audit=True)`）。
    - `_run_partner`：code/creation 经既有 `creation_domains.dispatch` 产出结构化蓝图（LLM 驱动或规则兜底）；research/ops 走规则简报/执行计划（有 `llm_call` 则升级）；均产出 `PartnerResult(summary/plan/artifacts)`。
    - `merge(results)`：**汇聚去重 + 冲突检测**（多伙伴产出同类 artifact 标 warning）+ 统一 struct-panel Markdown（✅/❌ 各伙伴 + ⚠ 冲突）。
  - `server.py`：新增 `GET /api/federation`（伙伴清单）、`POST /api/federation/dispatch {goal, hint_domains?}`（路由+并行派发+汇聚，注入 `_make_llm_call` 兜底 None）、`GET /federation` 页面；对应 `_federation_get` / `_federation_dispatch` 两方法。
  - `federation.html`（Nebula 暗色单页）：4 KPI（伙伴数/累计派发/最近派发域/冲突数）+ 伙伴清单卡（emoji/域/工具标签）+ 目标派发表单（goal + 可选 hint_domains）+ 协同结果（每伙伴卡 + 产出折叠 + 冲突告警）+ 汇聚报告（struct-panel）；6s 轮询 `/api/federation`。
  - `selfcheck.py`：导入探针加 `federation`；静态资产清单加 `web/static/federation.html`；新增 `check_federation`（4 伙伴 + 跨域路由 + 无 LLM 派发闭环全 ok），现 **11 个探针全绿**。
  - 测试：`tests/test_federation.py` **11 例**（路由单域/跨域/显式 hint / 并行派发无 LLM / 伙伴异常隔离 / 汇聚冲突检测 / 服务端 API + 页面 / 自检计数 11）；全量 pytest 回归 **546 passed / 36.4s**（0 failed）。
  - **未上线(需重打包)**：federation.py + server.py `/api/federation*` + federation.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。看最新页用「启动源码面板.bat」(8320)。

- **Phase 26 关键落地（长期记忆图谱 · Memory Graph）**：
  - `memory_graph.py`（新增，纯标准库 + sqlite3）：`Entity(name,type,confidence,trusted,last_used)` / `Relation(src,rel,dst,weight,confidence)` / `Fact(subject,ts,session_id,evidence,confidence)` 三件套 + `MemoryGraph`（本机 `.lmw_memory_graph.db`）。
    - `absorb(goal, result, llm_call?)`：规则抽取（命中「决定采用/约定/故障/接口/项目」等强信号归类 entity，连接词「依赖/来自/冲突/用于」抽 relation 三元组），有 `llm_call` 走 LLM JSON 抽取(失败回退规则)；实体以 `(name,type)` 复合主键去重；每条实体/关系落 `facts` 审计链。**隐私**：`_redact` 把 `api_key=sk-xxx` 等密钥值脱敏为 `<REDACTED>`，真值永不入图（仅记「接口/需 key」事实）。
    - `recall(goal, limit?)`：目标 token（中英混合 bigram）与实体名/类型重叠打分取 top-k + 命中实体 1 跳关系扩展，产出 `recap` Markdown（可注入 system prompt 的「历史经验」），并 bump `last_used`。
    - `decay(factor)`：非 trusted 实体/关系置信度衰减；`trusted` 实体豁免（铁律永驻）。
    - `export_markdown()`：可读报告（概览 + 类型分布 + 实体/关系清单，中文类型标签）。
  - **联邦沉淀联动**：`_federation_dispatch` 派发成功后自动调 `memory_graph.absorb(goal, merged_summary)` 入图（异常隔离，不阻塞主流程），并 emit `memory` 审计事件——收口「任务结束→记忆沉淀→新目标召回」闭环。
  - `server.py`：新增 `GET /memory-graph`（页）+ `GET /api/memory-graph`（统计+实体/关系）+ `POST /api/memory-graph/absorb`（沉淀）+ `POST /api/memory-graph/recall`（召回）；连接 `check_same_thread=False`（服务端 ThreadingHTTPServer 每请求独立线程，单例连接需跨线程可用）。
  - `memory_graph.html`（Nebula 暗色）：4 KPI（实体/关系/事实/可信）+ 实体清单卡 + 关系清单卡 + 沉淀表单(goal+result) + 召回表单(goal→实体+关系+recap) + 8s 轮询。
  - `selfcheck.py`：导入探针加 `memory_graph`；静态资产加 `web/static/memory_graph.html`；新增 `check_memory_graph`（内存库抽取多类实体 + 隐私脱敏 + 召回），现 **12 个探针全绿**。
  - **环境坑修复**：① 本环境 safe-delete 垫片拦截临时目录 `shutil.rmtree`→sqlite 临时库文件锁(WinError 32)清理失败 → 单元/探针测试统一改用 `:memory:` 内存库规避；② sqlite `lastrowid` 是 cursor 属性非 connection → 修正为 `cur.lastrowid`；③ 复合主键 `(name,type)` 避免同类文本多类型被 last-write 覆盖。
  - 测试：`tests/test_memory_graph.py` **11 例**（实体增改/关系抽取/多类抽取/隐私脱敏/关系三元组/召回+1跳/衰减 trusted 豁免/导出 md/服务端 GET+absorb+recall+页面+误用400/自检计数12）；`test_federation.py` 自检计数断言 11→12；全量 pytest 回归 **557 passed / 0 failed / 38.2s**（Phase 26 净增 11 例）。
  - **未上线(需重打包)**：memory_graph.py + server.py `/api/memory-graph*` + memory_graph.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。看最新页用「启动源码面板.bat」(8320)。

- **Phase 27 关键落地（超级 AGENT 内核 · Super-AGENT Kernel）**：
  - `superagent.py`（新增，纯标准库 + 复用 federation/memory_graph/selfcheck/event_bus）：`SuperAgent` 收口六阶段统一闭环——**① 目标理解** `understand(goal, llm_call?)`：有 `llm_call` 抽取 `{intent, domains, constraints}` JSON（失败回退 `federation.route` 规则路由）；并调 `memory_graph.get_graph().recall(goal)` 注入历史经验 `memory_recap`；**② 域路由** `route()` 直接取 `understand["domains"]`；**③ 并行编排** `dispatch()` 调 `federation.get_federation().dispatch(goal, hint_domains=domains)` 多伙伴并行派发；**④ 三级护栏收敛** `converge(dispatch_rep, quality_gate=True)`：1 级伙伴 error 隔离统计 / 2 级 merge 冲突统计 / 3 级 `selfcheck.run()` 评分 <70 告警；返回 `ok/partners_ok/conflicts/selfcheck_score/guards/passed`；**⑤ 自检质量门** `_quality_gate()`（try/except 满分兜底，杜绝递归）；**⑥ 记忆沉淀** `deposit_memory()` 调 `memory_graph.absorb(goal, merged_summary)`（异常隔离）；统一入口 `run(goal, session_id?, llm_call?, quality_gate=True)` 每阶段 `emit("superagent","stage",...,audit=True)` 进审计链，`result` 含 intent/routed/dispatch/converge/selfcheck/memory/trace/elapsed_sec，异常捕获不崩；`base_dir=None`→cwd 单例，测试用临时目录隔离；进程内 `_RUNS=deque(maxlen=60)` 留存最近编排。
  - `memory_graph.py` 增强：`get_graph(base_dir=None)` 加 `:memory:` 特判（返回独立内存库 `MemoryGraph(":memory:")`），规避临时目录 SQLite 文件锁（safe-delete 沙箱干扰），selfcheck 探针改用纯内存库。
  - `server.py`：新增 `GET /superagent`（页）+ `GET /api/superagent`（最近 20 编排）+ `POST /api/superagent/run {goal}`（缺 goal→400；`SuperAgent(base_dir=os.getcwd()).run(goal, llm_call=self._make_llm_call())`）；对应 `_superagent_get` / `_superagent_run` 两方法。
  - `superagent.html`（Nebula 暗色单页）：4 KPI（最近编排/跨域编排≥2伙伴/产出冲突/自检均分）+ 目标 textarea + 启动编排按钮 + 最近编排列表 + 结果区（意图理解 / 分阶段 trace 时间线 / 伙伴并行结果 / 三级护栏收敛 / 记忆沉淀）；5s 轮询 `/api/superagent`；标题正文均用「超级 AGENT」。
  - `selfcheck.py`：导入探针加 `superagent`；静态资产加 `web/static/superagent.html`；新增 `check_superagent`（用 `SuperAgent(base_dir=":memory:").run(goal, quality_gate=False)`——避免质量门递归调用 selfcheck.run + 内存库隔离），断言 ok / routed≥2 / 派发 ok≥2 / 收敛 partners_ok≥2 / 记忆沉淀 dict / trace≥5，现 **13 个探针全绿**。
  - 测试：`tests/test_superagent.py` **10 例**（目标理解规则兜底 + LLM 抽取 / 三级护栏收敛 / 质量门递归防护 / 服务端 API + 页面含「超级 AGENT」+ traceBox + partnerResult / 自检计数 13）；`test_selfcheck.py` 与 `test_federation.py` 与 `test_memory_graph.py` 探针计数断言 12→13；全量 pytest 回归 **566 passed / 0 failed / ~49s**（Phase 27 净增 10 例）。
  - **环境坑修复**：① selfcheck `check_superagent` 初版整块替换留 8 空格缩进残行导致 IndentationError → 统一 4 空格对齐 `rep = sa.SuperAgent(...).run(...)`；② 测试页面断言 `id="runTrace"` 与 HTML 实际 `id="traceBox"` 不一致 → 修正测试断言匹配真实 id；③ 探针计数 12→13 三处测试断言同步（selfcheck/federation/memory_graph）。
  - **未上线(需重打包)**：superagent.py + server.py `/api/superagent*` + superagent.html 前端, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。看最新页用「启动源码面板.bat」(源码 8318/8320 任一)。

- **Phase 28 关键落地（执行落地 · Executor & 可插拔执行器）**：
  - `superagent.py`：新增**域执行器注册中心** `EXECUTORS={}` + `register_executor(domain, fn)` / `get_executor(domain)`（后续可热插拔 `code→autonomous` / `creation→multimodal` / `research→fetch` / `ops→shell` 等真实执行器）；`SuperAgent` 新增 `execute(dispatch_rep, ...)` 阶段——每个 status==ok 的伙伴：若注册了 domain 执行器则调用（可走真实能力），否则走 `_default_executor` 把方案/蓝图写成**真实交付文件** `outputs/superagent/<ts>_<域>.md`（可下载/回看）；**异常隔离**（单伙伴失败不影响其他）；`base_dir=":memory:"` 或非法路径自动落临时目录，规避内存库名当路径；结果回写 `result["executions"]/["artifacts"]` 与 `_RUNS` 概览。
  - `run()` 阶段顺序升级为 **目标理解 → 域路由 → 并行编排 → 执行落地 → 收敛护栏 → 记忆沉淀**（6 阶段全进 trace + event_bus 审计链）。
  - `superagent.html`（Nebula 暗色）：新增「执行落地（executor · 产物）」卡片，按域渲染执行状态（✅ok/artifact / ❌error / 跳过）+ 交付文件路径链接（`<a href=绝对路径 target=_blank>` 可点开）。
  - `selfcheck.py`：`check_superagent` 探针加**执行落地断言**（默认执行器产出 >=2 真实交付文件且 `os.path.isfile` 校验），仍 **13 探针全绿**（Phase 28 未新增探针，只在既有探针内加断言）。
  - 测试：`tests/test_superagent.py` **扩至 13 例**（新增 默认执行器落真实文件 / 真实执行器注入调用 / 伙伴异常隔离跳过）；`test_server_api` 页面与 API 全链路仍 PASS（trace 现 6 阶段）；全量 pytest 回归 **569 passed / 0 failed / ~105s**（Phase 28 净增 3 例）。
  - 源码烟测（chdir 临时目录，避开 8319 僵尸端口，用 8391）：`POST /api/superagent/run` 200 → trace 6 阶段齐全、跨域路由 `[creation, ops]`、执行落地 2 域、产出 2 个真实 `.md` 交付文件（均 `os.path.isfile` 校验通过）、`result` 含 `executions` 键；SMOKE_RESULT=PASS。
  - **环境坑（本次新踩）**：① 烟测初版撞 8319 端口**僵尸源码服务**（Phase 27 遗留）返回旧版 response（无 executions / 仅 5 阶段）→ 换 8391 干净端口即通过；② 测试里 `"gen_code.txt" in list` 是**列表成员判断**非路径子串 → 改为 `any("gen_code.txt" in a for a in artifacts)`；③ 默认执行器落盘路径须规避 `:memory:` 当目录 → 用 `tempfile.mkdtemp()` 兜底。
  - **未上线(需重打包)**：superagent.py + server.py `/api/superagent*` + superagent.html 执行落地改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。看最新页用「启动源码面板.bat」(源码 8318/8320 任一)。

- **Phase 29 关键落地（真实执行器热插拔 · Real Executors by Default）**：
  - `superagent.py`：Phase28 的 `EXECUTORS={}` 默认空（域未注册走 `.md` 兜底）。Phase29 新增 **4 个模块级真实执行器并默认注册**（`register_executor("code"/"research"/"ops"/"creation", ...)`），内核从「出方案 md」升级为「出可验证真实产物」；`execute()` 调用签名不变（`fn(partner, goal=, llm_call=, base_dir=)`），`register_executor` 仍可覆盖为更智能实现（如 code→autonomous 真编码、creation→multimodal 真产出）。
    - `_exec_code`：**有 LLM 时让 LLM 产出 Python 实现并 `compile()` 校验**；无 LLM（无代码块）产出**可编译骨架** `code_skeleton.py`（`compile` 通过）。从伙伴 `plan` 抽取 ```python 代码块写成 `.py` 并编译，py 语法警告不致命。
    - `_exec_creation`：产出**可回读 `asset_manifest.json`**（sub_domain/prompt/spec/adapter_hint），供 multimodal 适配层消费生成真实素材。
    - `_exec_research`：设 `LMW_SA_ALLOW_FETCH=1` 时 `urllib` **真抓取**（DuckDuckGo 检索，5s 超时，落 `research_fetch.md`）；否则**落地研究简报 `.md`**（无网络不挂起）。
    - `_exec_ops`：把方案步骤落为**可 `bash -n` 校验的 `deploy.sh`**（shell 不可用时跳过校验但照写文件）。
    - 公共：`_resolve_out_dir`（base_dir 合法目录则用，否则 `tempfile.mkdtemp()` 兜底 `:memory:`/None）、`_extract_code_blocks`/`_extract_steps`/`_derive_research_url`/`_shell_syntax_check`；执行器**异常隔离**（单执行器失败不影响其他，状态标 error）。
  - `superagent.html`：「执行落地」卡片升级——按域展示**产物类型标签**（代码/素材清单/研究简报/运维脚本）+ 文件名 chip + **可执行动作提示**（code→py_compile/运行、creation→喂 multimodal、research→阅读/二次检索、ops→bash 校验/执行）；产物为服务器本地文件，渲染为 chip 而非失效链接。
  - `selfcheck.py`：`check_superagent` 探针加**「真实产物 >=1」断言**（默认执行器产出的 `asset_manifest.json`/`deploy.sh`/`code` 等应真实落盘且 `os.path.isfile`），仍 **13 探针全绿**。
  - 测试：`tests/test_superagent.py` **扩至 19 例**（新增 真实执行器注册断言 / code 编译落盘 / code 无块出骨架 / ops 脚本落盘 / creation manifest 回读 / research 未开启抓取落地简报 / server_api 加真实产物落盘校验）；全量 pytest 回归 **575 passed / 0 failed / ~117s**（Phase 29 净增 6 例）。
  - **未上线(需重打包)**：superagent.py 真实执行器 + server.py + superagent.html 改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。看最新页用「启动源码面板.bat」(源码 8318/8320 任一)。

- **Phase 30 关键落地（自主编码执行器 · Autonomous Code Executor）**：
  - `superagent.py`：`_exec_code` 从「编译校验」升级为**自主编码闭环**——
    - 新增 `_run_code_smoke(path, ext, out_dir)`：子进程冒烟运行生成代码（py→`sys.executable`、js→node（可用时）、sh→bash/sh），**15s 超时**、输出截断、`PYTHONIOENCODING=utf-8` 规避 Windows cp936 中文乱码/崩溃；无运行时语言标记 `skipped`。
    - 新增 `_smoke_and_heal(...)`：冒烟失败且 LLM 在场（仅 py）→ 把运行报错喂回 LLM 修正重跑，**最多 2 次**（`healed` 标记自修复成功）；产出结构化 `log_text` 与人话 `note`（通过/失败/跳过/自修复）。
    - `_exec_code`：每个代码块落盘后紧跟冒烟运行，`.run.log` 落为**独立产物**纳入 artifacts；无 LLM 兜底骨架从「可编译但 main raise NotImplementedError」改为**可运行骨架**（main 打印 `[scaffold] 骨架就绪` 并 `return 0`，`raise SystemExit(main())`），冒烟通过；返回体新增 `run_ok`（True/False/None=跳过）与 `healed` 字段。
  - `superagent.html`：执行卡片新增冒烟徽标（`▶ 冒烟通过·自修复` 绿 / `▶ 冒烟失败` 红，`run_ok=null` 不显示）；code 域动作提示改为「代码可运行 · 附 .run.log 冒烟日志(失败自动喂回 LLM 自修复)」。
  - 测试：`tests/test_superagent.py` **扩至 22 例**（新增 冒烟日志 returncode=0 / 可运行骨架不含 NotImplementedError / 自修复闭环（首产坏代码→LLM 修正→run_ok+healed）/ 无 LLM 坏代码如实 run_ok=False 且 run.log 含报错）；全量 pytest 回归 **579 passed / 0 failed / ~155s**（Phase 30 净增 4 例）。
  - **未上线(需重打包)**：superagent.py + superagent.html 改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

- **Phase 31 关键落地（真实创作执行器 · creation→multimodal 真产出）**：
  - `superagent.py`：新增 `_exec_creation_real`（默认注册为 creation 域执行器）与 `_creation_subdomain` 助手——
    - **真实渲染**：经既有 `multimodal_adapters.render(sub, brief=goal, blueprint=plan, out_dir=..., llm_call=...)` 真实产出媒体文件（image→Pillow PNG 信息图 / audio→优先 edge_tts MP3、不可用降级文字稿+声波占位 / video→Pillow 多帧 GIF），无 LLM 自动确定性模板兜底、全程可用；产出结果（file/mime/real/note/meta）回写进 manifest 的 `render` 键。
    - **子域路由**：优先伙伴蓝图标注（`artifacts[0].domain`），无标注按目标关键词路由（视频/动图→video、音频/配音/朗读→audio、默认 image）。
    - **降级链**：适配层 import 失败 / render 抛错 / 返回无文件 → 回退纯素材清单 JSON（旧 `_exec_creation` 行为），单伙伴失败不影响整体（执行器异常隔离不变）；旧 `_exec_creation` 保留，可 `register_executor("creation", _exec_creation)` 一行回切。
  - `superagent.html`：creation 产物类型「素材清单」→「创作产物」，动作提示更新为「真实媒体渲染 PNG/MP3/GIF(无 key 降级占位) + 素材清单 manifest」；副标题同步 Phase 31。
  - 测试：`tests/test_superagent.py` **扩至 25 例**（新增 真实媒体落盘（.png/.gif/.mp3 任一 + manifest 含 render 键）/ 适配层 monkeypatch 抛错回退纯清单不崩 / 无蓝图标注按关键词路由 video）；全量 pytest 回归 **582 passed / 0 failed / ~181s**（Phase 31 净增 3 例；creation 真实渲染使全量耗时 +~26s 属预期）。
  - **未上线(需重打包)**：superagent.py + superagent.html 改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。看最新页用「启动源码面板.bat」(源码 8318/8320 任一)。

- **Phase 32 关键落地（插件中枢 · Plugin Hub — Connector/Expert 注册 + 可用性降级 + 自动接入 AGENT）**：
  - 新增 `lingmengwork/plugin_hub.py`（零三方依赖）：
    - `Connector(name, category, description, call_fn, env_required, optional_dep, extra)`：声明外部工具能力，`check()` 自动检测 env 缺失并降级 unavailable（不崩），`call(goal, **kw)` 调用 handler。
    - `Expert(name, domain, system_prompt, description, tags)`：LLM 专家画像，供超级 AGENT 路由 hint + 执行器提示使用。
    - `PluginHub` 进程内单例（`get_hub()`）：`register_connector/register_expert/list_connectors/list_experts/get_connector/get_expert/discover/wire`；`discover(plugin_dir)` 扫描目录自动执行模块 `register_connectors(hub)`/`register_experts(hub)` 回调；`wire(superagent, goal)` 注入可用连接器 + 专家域提示。
    - **内置 bootstrap**（`_bootstrap()` 首次 `get_hub()` 触发，env 无关）：3 连接器（`health`→selfcheck / `recall`→memory_graph / `recent_runs`→superagent 概览）+ 3 专家（`codereview`→code / `design_lead`→creation / `ops_sre`→ops）。所有接入 emit 进 event_bus 审计链。
  - `superagent.py`：新增 `_wire_plugins(goal, understand)` 方法 + `run()` 新增「插件接入」阶段（目标理解之后、域路由之前）——可用专家域**并入** `understand["domains"]`（上限 4，不覆盖 LLM 抽取结果），wire 摘要记录在 trace + `result["plugins"]`，`_STAGE_NAMES` 增一阶至 7 阶段；失败静默不阻塞。
  - `server.py`：新增 8 个端点——`GET /plugins`（页面）、`GET /api/plugins`（概览）、`GET /api/plugins/connectors[?category=]`、`GET /api/plugins/experts[?domain=]`、`POST /api/plugins/discover {plugin_dir}`、`POST /api/plugins/wire {goal}`、`POST/GET /api/plugins/connectors/<name>`（直调连接器）、`POST /api/plugins/connectors/register` 与 `/experts/register`（动态注册）。
  - 前端 `web/static/plugin_hub.html`（Nebula 暗色）：4 KPI（连接器/可用/专家/降级）+ 快速操作（一键接入 + 目录扫描）+ 连接器表（全/可用/降级 三档筛选 + 动态注册）+ 专家列表（含动态注册）；5s 轮询。
  - `selfcheck.py`：`_STATIC_FILES` 加 `plugin_hub.html`；`check_imports` 加 `plugin_hub`（9→10 模块）；新增 `check_plugin_hub` 探针（默认 3+ 连接器/3+ 专家 + env 降级验证 + wire 接入不崩），探针 13→**14 全绿**。
  - 测试：`tests/test_superagent.py` **扩至 29 例**（新增 默认 bootstrap 3+3 / env 降级 / wire 融入超级 AGENT 编排 / 服务端 /plugins 页面 + /api/plugins 端点 HTTP 可达）；`test_selfcheck/test_federation/test_memory_graph` 探针计数 13→14；全量 pytest **586 passed / 0 failed / ~190s**（Phase 32 净增 4 例）。
  - **未上线(需重打包)**：plugin_hub.py + superagent.py + server.py + plugin_hub.html 改动, 冻结 exe(8318) 不含; 按纪律需用户许可才 PyInstaller rebuild。

- **Phase 33 关键落地（研究执行器升级 · 默认真抓 + LLM 摘要）**：
  - `superagent.py` `_exec_research` 去掉 `LMW_SA_ALLOW_FETCH=1` 前置门——默认走真实 DuckDuckGo 抓取（`html.duckduckgo.com/html/?q=`，8s 超时，200KB 上限）；网络/超时失败才回退研究简报 `.md`；**有 LLM 时**对抓取内容（或失败时方案）做中文要点摘要（3-5 条），摘要落独立 `.md` 产物；`_urllib_req` 提到模块级以支持测试 monkeypatch。
  - 新增 `_derive_research_url` 对空目标友好处理（返回空串 → 跳过抓取直接落简报）。
  - 测试：`test_exec_research_fallback` 重命名为 `test_exec_research_fetch_fallback`（mock urllib 异常触发回退）+ 新增 `test_exec_research_fetch_success`（mock 成功 → 验证 research_fetch.md + LLM 摘要）+ `test_exec_research_empty_goal`；全量 pytest **588 passed / 0 failed / ~205s**（Phase 33 净增 2 例）。
  - 验证：research 域从「规则简报」升级为「默认真实抓取 + 可选 LLM 摘要」，无网络不挂起，无 LLM 不降级。

- **Phase 34 关键落地（联邦路由 + 连接器能力标签匹配）**：
  - `plugin_hub.py`：Connector 新增可选 `tags` 字段（能力关键词列表）+ `to_dict()` 输出 tags；新增 `match_connectors(goal)`——按目标关键词（中英分词）匹配可用连接器 name/category/tags，返回 [{name, category, description, tags}]；降级连接器自动排除。
  - `federation.py`：`dispatch()` 新增 `connector_names` 参数——传入预匹配连接器名单，逐一 `call()` 调用（异常隔离），结果结构 `matched_connectors=[{name, ok, result, error}]` 注入返回体；连接器调用 emit `connectors_done` 审计事件。
  - `superagent.py`：`_wire_plugins()` 新增 `match_connectors(goal)` 调用，结果存入 `understand["connectors"]`(name 列表) + `understand["matched_connectors"]`(完整信息)；`dispatch()` 透传 `connector_names`；并行编排 trace 加「连接器调用 N」。
  - `federation.html`：KPI 条加「连接器调用(Phase 34)」计数字；协同结果区新增 `.ccard` 连接器卡（每个连接器状态徽章 + 名称 + 结果/错误）；5 列 KPI 网格。
  - 测试：`tests/test_federation.py` 新增 3 例（match_connectors 命中/降级排除 / dispatch 调用连接器 / 空目标）；全量 pytest **591 passed / 0 failed / ~258s**（Phase 34 净增 3 例）。
  - 设计约束：连接器匹配纯关键词规则(无 LLM 亦可用)；匹配/调用失败不影响伙伴派发(异常隔离)；`tags` 字段向后兼容(可选)。

- **Phase 35 关键落地（超级 AGENT 全链路可观测合规）**：
  - `web/server.py` `_global_stats()` 追加 `superagent` 字段（`_superagent_stats()`）：从 `superagent._RUNS` 环形缓冲读最近 30 次编排，聚合出 total/ok/fail + 耗时分位(elapsed_p50/p95/max) + 自检均分(avg_score) + 最近 10 条明细(goals/routed/partners/conflicts/artifacts/elapsed/score/guards)。
  - `web/static/observability.html` 新增「🤖 超级 AGENT 编排」区块：8 KPI 卡(累计/成功/失败/成功率/中位/p95/最长/均分) + 最近编排表格(时间/目标/状态/路由/伙伴/冲突/产物/耗时/自检)；`renderSuperagent()` 消费 stats.superagent；`loadAll()` 触发渲染。
  - 设计约束：无编排数据时返回零态不崩；观测端读缓冲而非实时派发, 不引入性能负担；异常隔离(suppress)。
  - 测试：`tests/test_observability_page.py` 新增 2 例(页面含 superagent 区+renderSuperagent / _superagent_stats 空态+有数据聚合正确)；全量 pytest **593 passed / 0 failed / ~164s**（Phase 35 净增 2 例）。
  - 收口「全链路可观测合规」: 工具调用(工具级分位/成功率)+ 健康度(MCP/LLM/文件系统)+ 成本(会话级 token)+ 超级 AGENT 编排(全 7 阶段聚合 + 明细), observability 页四件套齐全。

- **Phase 36 关键落地（真实连接器示例 + 端到端标签匹配验证）**：
  - 新增 `lingmengwork/plugins/__init__.py`(空, 使 plugins 成为合法包)。
  - 新增 `lingmengwork/plugins/sample_http_probe.py`：HTTP 端点探测连接器(18 tags: http/web/endpoint/probe/diagnose/network/health/ping/check/status/tls/http-request/response/latency/reachability/诊断/网络/端点/可达性/超时/SSL/延迟)。`call_fn(goal, **kw)` 从 goal/kw 提取 URL(正则 + 中文标点兼容)，HEAD 探测优先 → GET 回退，8s 超时，返回 `{ok, url, status, elapsed_ms, content_type, size, error}`；`register_connectors(hub)` 统一注册。
  - 修 `plugin_hub.py` `match_connectors` 分词：双层切分——先按 ASCII 空白+中文标点切，再在 CJK/非CJK 边界切(解决「系统health」粘连)；对 CJK 片段展开 bigram(兼容无标点中文目标)。三种目标形态全覆盖：纯中文/纯英文/中英混合无空白。
  - 新增 `tests/test_phase36_http_probe.py` 7 例：call_fn 默认目标回退 / 指定 URL / URL 提取 / kw 覆盖 / discover 目录扫描 / match 中文标签命中 / federation dispatch 端到端调用。
  - 全量 pytest **600 passed / 0 failed / ~258s**（Phase 36 净增 7 例, 含修复 2 个回归）。
  - 收口「插件中枢 → 标签匹配 → 联邦路由 → 连接器调用」完整闭环：注册 → 扫描 → 匹配(规则,无 LLM 可用)→ 派发(异常隔离)→ 观测可查。

- **Phase 37 关键落地（真 LLM 端到端编排验证）**：
  - 新增 `tests/test_phase37_e2e.py` 4 例：
    - `test_e2e_full_pipeline_with_llm_and_connector`：mock LLM(按 system 语境分发: 任务理解器/研究分析师/运维/知识图谱抽取器) + 真实连接器 http_probe 探测**本地真实 HTTP 端点**(ThreadingHTTPServer, 200) + 全 7 阶段 trace 顺序断言(== `_STAGE_NAMES`) + LLM intent/约束抽取 + 多域路由 + 交付文件落盘 + 记忆实体入图 + `_RUNS` 观测留痕。
    - `test_e2e_no_llm_rule_fallback`：无 LLM 规则兜底全链路(关键词路由 research+ops, 连接器空匹配不阻塞)。
    - `test_e2e_kernel_exception_isolated`：dispatch 抛异常 → ok=False + trace「内核异常」+ error 留痕 + `_RUNS` 记录失败, 不崩。
    - `test_federation_connector_dict_result_propagated`：连接器 dict 结果透传回归。
  - 修 `federation.py` 连接器结果透传缺口：call_fn 返回 dict 且含结构化字段(status_code/elapsed_ms 等)时整包透传, 纯 `{"ok","name","result"}` 包装仍取 result 摘要——此前探测数据全被 `cr.get("result")` 丢弃只剩摘要串或 None。
  - `federation.html` 连接器卡片：dict 结果渲染为 JSON 字符串(此前 `[object Object]`)。
  - 测试基建：`EXECUTORS` 全局注册表 monkeypatch 整体替换(快速执行器, 隔离真实研究抓取/编码冒烟)；selfcheck 质量门 monkeypatch 96 分。
  - 全量 pytest **604 passed / 0 failed / ~273s**（Phase 37 净增 4 例）。
  - 收口「真 LLM 端到端编排验证」：全 7 阶段(目标理解→插件接入→域路由→并行编排→执行落地→收敛护栏→记忆沉淀)在 mock LLM + 真实连接器 + 真实 HTTP 端点下闭环验证通过；配真实 SENSENOVA key 即为生产形态(无 key 规则兜底同链路)。

- **Phase 38 关键落地（超级 AGENT SSE 流式编排 · 实时进度）**：
  - `superagent.py` `run()` 新增可选 `on_stage(entry)` 回调：每阶段完成即触发 `{stage, ts, ok, detail}`，回调异常隔离不阻塞主流程；向后兼容(不传行为不变)。
  - `web/server.py` 新增 `POST /api/superagent/run/stream`：SSE 流式编排——与同步端点同链路(规则兜底/异常隔离/审计链)，逐阶段推送 `{"type":"stage",...}`，最终 `{"type":"done","result":{完整编排结果}}`；缺 goal 推 `{"type":"error"}`；客户端断开置 flag 停止推送(编排继续跑完不空转)。
  - `web/static/superagent.html` `runSuper()` 改走流式端点：ReadableStream 逐帧解析 SSE，每阶段实时上屏(appendStage 逐条插入 trace 区)，done 后渲染完整结果；fetch/流异常自动回退同步 `/api/superagent/run`。
  - 测试：`tests/test_phase38_superagent_stream.py` 4 例(on_stage 回调与 trace 逐条一致 / 回调异常隔离 / SSE 端点 7 阶段事件+done+缺 goal error / 页面含流式 UI 与同步回退)。
  - 全量 pytest **608 passed / 0 failed / ~287s**（Phase 38 净增 4 例；另确认 test_server_review_report_e2e 的 socketserver 异常打印为存量噪音，与本次无关）。
  - 收口：编排从「黑盒等待→一次性结果」升级为「实时阶段进度流」，配真实 LLM 长编排(数十秒)时体验对齐 /api/chat SSE。

- **Phase 39 关键落地（编排历史持久化 · 重启不丢可回看）**：
  - `superagent.py`：`_record()` 同 ts 双写——内存 `_RUNS` 摘要 + `_persist_result()` 落盘 `<base>/outputs/superagent_runs.jsonl`（行结构 `{ts, summary, result完整编排结果}`）；单条 64KB 体积保护（超限递归截断超长字符串 `_clip_strings`，仍超限丢弃 dispatch/executions/intent 重载荷）；持久化异常静默。
  - `get_recent_runs(limit, base_dir)` 升级：内存优先 + 磁盘补缺（goal+ts 去重；磁盘仅补充内存没有的更早记录，同秒平局不让磁盘行插到内存最新之前）；重启后内存清空、历史从磁盘回看。
  - 新增 `get_run_detail(ts, base_dir)`：按 ts 取单次完整编排结果。
  - `web/server.py`：`/api/superagent` 概览带 base_dir；新增 `GET /api/superagent/detail?ts=`（缺 ts 400 / 未找到 404）。
  - `superagent.html`：历史记录可点击 → `loadRunDetail(ts)` 拉取完整结果复用 `renderResult` 渲染 + 平滑滚动定位。
  - 测试：`tests/test_phase39_persist.py` 6 例（JSONL 落盘结构 / 模拟重启磁盘回看+ts 倒序 / detail 未找到 None / 体积保护 <64KB / detail API e2e 含 400/404 / 页面回看 UI）；`test_observability_page` 空态测试补 `_persist_path` 隔离。
  - 全量 pytest **614 passed / 0 failed / ~184s**（Phase 39 净增 6 例；`.gitignore` 增加 `outputs/superagent_runs.jsonl`）。
  - 收口：编排历史从「内存环形缓冲重启即失」升级为「JSONL 持久化 + 页面一键回看详情」。

- **Phase 40 关键落地（编排成本入账 · 超级 AGENT 用量进成本看板）**：
  - `superagent.py`：新增 `_UsageMeter` 计量器——包装 llm_call 统计调用次数/输入输出字符数，估算口径与 agent.loop 一致（~1.6 字符/token），成本按 `llm/pricing` 价格档（model 可空 → 默认档）；无后端不计数。`run()` 新增可选 `model=""` 参数；`result["usage"]` 记录 `{model, llm_calls, est_input_tokens, est_output_tokens, est_total_tokens, est_cost_cny}`（内核异常路径同样携带）。
  - `_record()` summary 增补用量字段（llm_calls / est_input_tokens / est_output_tokens / est_total_tokens / est_cost_cny）→ `_RUNS` 与 JSONL 持久化自动携带。
  - 新增 `get_usage_totals(limit, base_dir)`：内存优先 + 磁盘历史补缺聚合（重启不丢）。
  - `web/server.py`：`/api/superagent/run` 与 `/run/stream` 传 `model=self._cfg_llm_model()`；`_cost_stats()` 返回体新增 `"superagent"` 聚合段。
  - `cost.html` 新增「🤖 超级 AGENT 编排用量 (含历史)」区块：编排次数 / LLM 调用 / 估算总 Token / 估算总成本 四卡。
  - 测试：`tests/test_phase40_usage.py` 6 例（mock LLM 计量 / 无 LLM 全零 / summary+JSONL 携带 / 聚合重启不变 / cost API superagent 段 e2e / cost.html 渲染区块）。
  - 全量 pytest **620 passed / 0 failed / ~178s**（Phase 40 净增 6 例）。
  - 收口：编排 LLM 消耗从「不可见」到「入账成本看板 + 随历史持久化」，与 /api/chat 会话成本同口径同价目表。

- **Phase 41 关键落地（编排队列与并发控制）**：
  - `superagent.py`：进程级并发槽位（`threading.BoundedSemaphore`，默认上限 2）——`run()` 获取槽位有限排队（`queue_wait_sec` 默认 30s），超时忙拒绝返回 `{ok:False, busy:True}`（不进编排历史/不落盘）；槽位内主体拆入 `_run_core()`（由 run() 持锁调用，running/waiting 计数全程跟踪）。
  - `set_max_orchestrations(n)`（非法值收敛 1）+ `get_queue_state()` → `{running, waiting, max}`。
  - `web/server.py`：新增 `GET /api/superagent/queue`（每次查询顺带应用 config `agent.max_orchestrations`，缺省 2）；同步编排端点支持 body `queue_wait_sec`（钳 1–60s）且 busy 时返回 **429**；SSE 流端点 busy 时 emit `{"type":"error","busy":true}`。
  - `superagent.html`：「最近编排」标题旁实时显示并发 chip（`并发 running/max[ · 排队 N]`）。
  - 测试：`tests/test_phase41_queue.py` 6 例（上限设置/非法值收敛、占满忙拒绝不进历史、释放后恢复、running/waiting 计数与归零、queue API + 429 e2e、页面 chip）。
  - 全量 pytest **626 passed / 0 failed / ~179s**（Phase 41 净增 6 例）。
  - 收口：多目标同时编排从「无界并发互踩」到「进程级排队 + 忙拒绝 + 状态可视」，配 `agent.max_orchestrations` 可调。

- **Phase 42 关键落地（编排产物中心）**：
  - `web/server.py`：`GET /api/superagent/artifacts` 扫描 `outputs/superagent/` 清单（name/ext/size/mtime，按原始浮点 mtime 倒序——秒级字符串做排序键会同秒乱序）；`GET /api/superagent/artifacts/file?path=&mode=preview|download`——preview 返回文本 JSON（截 64KB + truncated 标记），download octet-stream 附件（RFC 5987 `filename*=UTF-8''` 支持中文名）。
  - 安全：`_artifact_safe_path()` 拒绝 `..`/绝对路径，realpath 必须严格落在 outputs/superagent 内（防目录穿越）。
  - `superagent.html` 新增「📦 产物中心」卡片：清单（文件名/类型/大小/时间）+ 预览按钮（内联 `<pre>` 弹出）+ 下载链接。
  - 顶部导入补 `quote as _urlquote`（修复 urllib.parse.quote 未导入的 NameError）。
  - 测试：`tests/test_phase42_artifacts.py` 3 例（清单倒序+字段 / 预览+下载字节一致+attachment 头+穿越 400 组 / 空目录+页面区块）。
  - 全量 pytest **629 passed / 0 failed / ~193s**（Phase 42 净增 3 例）。
  - 收口：编排执行落地产物从「散落磁盘不可见」到「面板内聚合浏览 / 预览 / 下载」。

- **Phase 43 关键落地（定时编排 · 调度器 + 计划管理）**：
  - `superagent.py` 新增调度层：计划 JSON 持久化 `outputs/superagent_schedules.json`（按文件路径幂等加载）；`add_schedule`（every_sec≥60 或 daily HH:MM，严格校验 `[01]?\d|2[0-3]:[0-5]\d`）/ `update_schedule`（白名单键）/ `remove_schedule` / `list_schedules`。
  - 到期判定 `_sched_due`：never-run 立即到期 / every_sec 按间隔 / daily 当天目标时刻已过且未跑 / disabled 永不。
  - `run_schedule`（同步执行 + last_run/last_ok/run_count 持久化，走 Phase 41 并发槽位 queue_wait_sec=5 忙则跳过）；`_scheduler_tick` 扫描到期 → 后台线程派发（`_INFLIGHT` 防同计划重复）；`start_scheduler`（daemon 20s tick，幂等）。
  - `web/server.py`：GET `/api/superagent/schedules` 列表；POST `create/update/delete/run`（400/404 语义齐全）；`run_web()` 启动常驻调度器（llm_call 工厂每次执行现取, 后端配置运行期变化即时生效）。
  - `superagent.html` 新增「⏰ 定时编排」卡片：目标 + 间隔分钟 / 每日时间 表单 + 计划列表（启用开关 / 立即执行 / 删除）。
  - 测试：`tests/test_phase43_scheduler.py` 6 例（CRUD+校验+持久化重载 / 到期判定 7 场景 / run_schedule 计数 / tick 派发 / API e2e 含 400/404 / 页面 UI）。
  - 全量 pytest **635 passed / 0 failed / ~272s**（Phase 43 净增 6 例）。
  - 收口：编排从「人手触发」到「定时自动触发」，与并发槽位/历史持久化/成本入账天然复用。

- **Phase 44 关键落地（编排模板库 · 参数化目标 + 一键发起）**：
  - `superagent.py` 新增模板层：JSON 持久化 `outputs/superagent_templates.json`（幂等加载）；`add_template`（name/goal_template 必填）/ `update_template`（白名单键）/ `remove_template` / `list_templates`（附 `fields` 占位符清单）。
  - 占位符引擎：`{{变量}}` 语法（支持中文变量名）；`template_fields` 保序去重提取；`render_template_text` 替换 + missing 缺失标记（缺失占位符原样保留）；`use_template` 渲染 + use_count/last_used 入账。
  - `web/server.py`：GET `/api/superagent/templates`；POST `create/update/delete/render`。
  - 🔴 踩坑修复：server 已有「提示词模板」的 `_templates_delete`（`/api/templates/delete`），同名方法类内互相覆盖 → 新方法统一改前缀 `_sa_tpl_*`，两套路由互不干扰。
  - `superagent.html` 新增「🧩 模板库」卡：名称+模板表单（`{{变量}}` 提示）+ 模板列表（一键发起 → 逐参数 prompt 填参 → render → 预填目标 → 走 SSE 流式编排）/ 删除 / 使用计数。
  - 测试：`tests/test_phase44_templates.py` 5 例（CRUD+校验 / 占位符提取与渲染含中文变量 / use 计数+持久化重载 / API e2e 含 400/404 / 页面 UI）。
  - 全量 pytest **640 passed / 0 failed / ~217s**（Phase 44 净增 5 例）。
  - 收口：常用编排目标从「每次手敲」到「模板一键发起自动填参」。

- **Phase 45 关键落地（模板 × 定时编排联动）**：
  - `superagent.py`：`add_schedule` 支持 `template_id`+`tpl_vars` — 创建时渲染校验（模板不存在/参数缺失 → ValueError），goal 落**渲染快照**；`run_schedule` 执行期**重渲染**（模板改动即时生效），模板已删/渲染失败**回退目标快照**（`_schedule_effective_goal`）；`update_schedule` 白名单扩 template_id/tpl_vars；`list_schedules` 附 `template_name`（已删模板留空）。
  - `web/server.py`：schedules create/update 透传 template_id/tpl_vars。
  - `superagent.html`：定时编排表单加「引用模板」下拉（选中后隐藏手填目标、动态生成模板参数输入），计划列表显示 🧩 模板名徽章；模板下拉随模板库自动同步。
  - 测试：`tests/test_phase45_linkage.py` 5 例（模板建计划含 400 校验 / 执行期重渲染+回退 / update 白名单 / API e2e / 页面 UI）。
  - 全量 pytest **645 passed / 0 failed / ~232s**（Phase 45 净增 5 例）。
  - 收口：模板库与定时编排打通 —— 「模板定义一次 → 定时自动带参执行 → 模板升级即时生效」。

- **Phase 46 关键落地（编排结果 Webhook 通知）**：
  - `superagent.py`：Webhook 接收端 JSON 持久化（`outputs/superagent_webhooks.json`，幂等加载）；`add/update/remove/list_webhooks`（url http(s) 校验, events: all|done|fail, secret 可选）；`_webhook_post` POST JSON + `X-LMW-Event` + 可选 `X-LMW-Signature: sha256=<HMAC-SHA256(body, secret)>`（5s 超时, last_status/last_ts 回写）；`notify_webhooks`（事件过滤匹配, 默认后台线程, 异常静默）。
  - 挂接点：`_run_core` 成功与内核异常两个出口均触发 notify（busy 忙拒绝不通知）。
  - `web/server.py`：GET `/api/superagent/webhooks`；POST `create/update/delete/test`（test 支持 id 或临时 url+secret, 发送 event=test 探针）。
  - `superagent.html` 新增「🔔 通知 Webhook」卡：URL + 事件选择 + 签名密钥表单；列表（启用开关/测试/删除/上次状态与时间）。
  - 测试：`tests/test_phase46_webhooks.py` 6 例（CRUD+校验 / 真实送达+签名可验证+last_status 回写 / 事件过滤 / 内核异常触发 fail 通知 / API e2e 含 test 探针 / 页面 UI）。
  - 全量 pytest **651 passed / 0 failed / ~260s**（Phase 46 净增 6 例）。
  - 收口：编排结果从「面板内可见」到「主动外推」——对接飞书/钉钉机器人或任意自定义接收端即可感知自动化编排结果。

- **Phase 47 关键落地（编排报告页面化 · 可分享 HTML）**：
  - `web/server.py` 模块级新增 `_render_orch_report(result)`：自包含内联 CSS 暗色 HTML 报告——目标+状态徽章+KPI(路由/伙伴/自检/耗时) / 🧭目标理解 / 📜7阶段Trace表 / 🤝伙伴结果 / 🔌连接器 / 🛡️收敛护栏 / 📦产物 / 🧠记忆 / 💰用量成本，全部 HTML 转义防注入。
  - `GET /api/superagent/report?ts=`：从磁盘编排历史取详情 → 渲染 text/html（inline 可浏览器直开/另存/归档分享）；缺 ts 400 / 未知 404。
  - `superagent.html` 历史编排每行新增「📄 报告」按钮（新标签页打开报告）。
  - 测试：`tests/test_phase47_report.py` 3 例（渲染区块+XSS 转义+失败路径 / API e2e 含 400/404 / 页面按钮）。
  - 全量 pytest **648 passed**（Phase 47 净增 3 例）。
  - 收口：编排结果从「面板内回看」到「一键生成可离线查看/归档/分享的正式报告」。

- **Phase 48 关键落地（Webhook 消息格式适配 + README 开源化）**：
  - `superagent.py`：`_WEBHOOK_FORMATS`（raw/feishu/dingtalk）+ `_webhook_wrap`（按接收端格式包装 payload）+ `_webhook_text`（编排结果压一句话摘要）；`add_webhook`/`update_webhook` 白名单加 `fmt`（非法回退 raw）；`_webhook_post` 按 fmt 包装后再签名/发送。
  - `web/server.py` create 透传 `fmt`；`superagent.html` Webhook 表单加「消息格式」下拉（原始 JSON / 飞书机器人 / 钉钉机器人），列表显示格式。
  - README.md 开源化：License/Python/Tests(660)/Platform 徽章 + GitHub/Gitee 双仓库链接 + 开源说明（MIT · 灵梦AI团队 · 欢迎 Fork/PR）。
  - 测试：`tests/test_phase48_formats.py` 6 例（三格式包装 / fail 摘要 / fmt 入库回退 / 飞书真实送达结构 / API fmt 透传 / README 开源元素）。
  - 全量 pytest **660 passed / 0 failed / ~301s**（Phase 48 净增 6 例）。
  - 收口：通知接收端从「仅原始 JSON」到「开箱即连飞书/钉钉群机器人」，README 达到开源门面标准。

- **Phase 49 关键落地（Webhook 富文本消息 + 报告链接直出群内）**：
  - `superagent.py`：`set_public_base_url()`（面板公开地址, server run_web 启动时注入, 优先 config `agent.public_base_url`）+ `_report_url_for(result)`（`<base>/api/superagent/report?ts=<ts>`）；`_record()` 回写 `result["ts"]` 供链接生成。
  - 飞书格式升级为 **interactive 卡片**：绿/红 header 模板 + markdown 正文（目标/状态/路由/耗时/自检分/错误）+ 「查看完整报告」primary 按钮。
  - 钉钉格式升级为 **markdown 消息** + `actionCard.singleURL` 跳转报告。
  - raw payload 内嵌 `report_url` 字段；`_webhook_text` 纯文本摘要同样附报告链接。
  - 测试：`tests/test_phase49_richtext.py` 5 例（链接生成+空态 / _record ts 回写+持久化一致 / 飞书卡片结构+失败红头 / 钉钉 markdown+actionCard / raw 内嵌）；Phase 48 测试同步适配富文本结构。
  - 全量 pytest **665 passed / 0 failed / ~194s**（Phase 49 净增 5 例）。
  - 收口：群机器人收到的不只是「跑完了」一句话，而是带状态色、自检分和**一键直达完整 HTML 报告**的富文本卡片。

- **Phase 50 关键落地（编排日报/周报 · 统计聚合 + Webhook 推送）**：
  - `superagent.py`：`_digest_stats(period)` 聚合最近 24h/7d 统计（总次数/成功/失败/成功率/平均耗时/平均自检分/LLM 调用/token/成本/最近目标 Top10）——**单源磁盘 JSONL**（按 base_dir 天然隔离, 不混入其它目录运行, _record 同步落盘保证数据完整）；`push_digest` 推送到全部启用接收端（events: all）。
  - digest 消息格式分支：飞书蓝色 header 卡片 + 钉钉 markdown（📊 日报/周报标题 + 统计正文）。
  - 每日自动：`set_digest_time("HH:MM")`（server 启动注入 config `agent.digest_time`, 空=不自动）→ scheduler tick 到点当天未发则后台推日报（`_maybe_push_daily_digest`）。
  - API：`GET /api/superagent/digest?period=`（预览, 非法 400）；`POST /api/superagent/digest/push {period}`。
  - `superagent.html` 定时编排卡尾部「📊 编排摘要」：预览日报/周报（内联统计框）+ 推送日报/周报按钮。
  - 测试：`tests/test_phase50_digest.py` 6 例（聚合统计/时刻校验/digest 包装/真实送达/API e2e 含 400/页面按钮）。
  - 全量 pytest **671 passed / 0 failed / ~194s**（Phase 50 净增 6 例）。
  - 🔴 教训：信封字段与业务字段撞名（信封 ok:True 被 stats ok:成功数 覆盖）→ stats 改名 ok_count；以及跨 base_dir 内存数据污染 → digest 单源取磁盘。
  - 收口：编排体系从「单次通知」到「周期性运营摘要」——日报/周报一键或定时直出群内。

- **Phase 51 关键落地（全局浅色/深色主题切换）**：
  - 新增 `static/theme.js`：浅色主题变量覆盖层（统一映射两套 CSS 变量体系 --tx/--txt、--card/--panel、--acc/--accent 等 20+ 变量）+ body 深色渐变背景 !important 压制 + 常见容器类（.card/.panel/.kpi/.run/input…）浅色化 + 右上角浮动切换浮球（☀️/🌙）+ localStorage(`lmw_theme`) 记忆、切换即生效零刷新。
  - 全部 31 个静态页面 `</body>` 前注入 `<script src="/static/theme.js">`（幂等, 带防重复检查）。
  - 测试：`tests/test_phase51_theme.py` 3 例（theme.js 内容/全页注入+无重复/静态服务可达 200）。
  - 全量 pytest **674 passed / 0 failed / ~181s**（Phase 51 净增 3 例）。
  - 收口：开源面板默认深色 Nebula，用户一键切浅色——外部用户体验完善。

- **Phase 52 关键落地（编排失败自动重试 · 指数退避）**：
  - `superagent.py`：`set_default_retry_max(n, backoff_sec)`（默认 0=不重试, 退避基数 5s）+ `run_with_retry()`——失败/busy 按 `backoff*2^(n-1)` 退避重试（封顶 60s），busy 时 queue_wait 压到 2s 快速轮转，返回结果附 `retries`。
  - 定时计划联动：`add_schedule`/`update_schedule` 支持 `retry_max`（0-5 钳位）；`run_schedule` 按计划 retry_max 自动重试（退避基数 2s），成功后记录 `last_retries`。
  - `web/server.py`：schedules create 透传 retry_max；`run_web` 注入 config `agent.orchestration_retry_max`。
  - `superagent.html`：定时表单加「失败重试(0-5)」输入，计划列表显示「失败重试 N」。
  - 测试：`tests/test_phase52_retry.py` 7 例（默认注入/负数收敛 / 失败后成功 retries=2 / 重试耗尽 / 默认不重试 / 计划级重试 last_retries / API 透传+run / 页面输入）。
  - 全量 pytest **681 passed / 0 failed / ~232s**（Phase 52 净增 7 例）。
  - 收口：无人值守的定时编排从「失败即弃」到「指数退避自动重试」，瞬态故障自愈。

- **Phase 53 关键落地（产物自动归档压缩 · 防磁盘膨胀）**：
  - `superagent.py`：`archive_old_artifacts(base_dir, max_age_days=30)`——`outputs/superagent/` 超龄产物打进 `archive_<YYYYMM>.zip`（DEFLATED, 追加模式, 重名加时间戳）后删除原文件；编排历史 JSONL 超龄行同步修剪进 zip（新行保留重写）；月度分 zip。挂入 scheduler tick 自动运行（异常静默）。
  - API：`POST /api/superagent/artifacts/archive {max_age_days?}`；`superagent.html` 产物中心 header 加「🗜️ 归档30天前产物」按钮（确认+结果提示+刷新列表）。
  - 测试：`tests/test_phase53_archive.py` 4 例（归档+删除+zip 内容+幂等 / JSONL 修剪保留新行 / API e2e / 页面按钮）。
  - 全量 pytest **685 passed / 0 failed / ~199s**（Phase 53 净增 4 例）。
  - 收口：编排产物与历史从「无限增长」到「超龄自动压缩归档」，zip 可随时下载解压回看。

- **Phase 54 关键落地（编排指标趋势图 · 按日曲线）**：
  - `superagent.py`：`get_daily_trend(days=14)`——单源磁盘 JSONL 按日聚合（date/total/ok/fail/success_rate/avg_elapsed/cost），日期升序，days 钳 1-90。
  - API：`GET /api/superagent/trend?days=14`。
  - `superagent.html` 新增「📈 编排趋势」卡：零依赖 SVG 堆叠柱状图（绿=成功/红=失败，悬停 title 看详情，柱顶成功率%），尾部汇总行（近 N 天总次数/成功/成本）。
  - 测试：`tests/test_phase54_trend.py` 4 例（跨日聚合/升序 / 空态+钳位 / API e2e / 页面区块）。
  - 全量 pytest **689 passed / 0 failed / ~197s**（Phase 54 净增 4 例）。
  - 收口：编排健康度从「数字列表」到「一眼看趋势」——成功/失败比例与周期波动可视化。


- **Phase 55 关键落地（编排结果结构化导出 · Markdown/JSON/ZIP）**：
  - `web/server.py`：`_render_orch_md(result)` 渲染器——把单次编排渲染成完整 Markdown 文档（🧭目标理解 / 📜阶段Trace 表 / 🤝并行编排表 / 🛡️收敛护栏 / 📦执行产物 / 🧠记忆沉淀+💰用量 / footer），表格单元格内管道符 `|` 转义为 `\|`、换行折叠为空格，**保证表格结构永不被内容撑破**。
  - API 三个新端点：`GET /api/superagent/export?ts=&fmt=md|json`（Content-Type text·markdown / application·json + `Content-Disposition: attachment` 附件下载，文件名 `lingmeng-orch-<ts>.md|json`）、`GET /api/superagent/export/bundle?ts=`（md+json+README.txt 打进 zip 一次带走）。非法 fmt / 缺 ts → 400，记录不存在 → 404。
  - `superagent.html`：结果面板顶部新增「结构化导出」工具条（⬇️Markdown / ⬇️JSON / 📦ZIP包 / 📄HTML报告 / 📋复制MD 一键写剪贴板，无 ts 时提示未落盘）；历史记录每行追加「⬇️ MD / ⬇️ JSON / 📦 ZIP」三个直达下载按钮（`event.stopPropagation()` 防误触详情回看）。
  - 测试：`tests/test_phase55_export.py` 7 例（章节结构 / 管道符+换行转义与列数不撑破 / 空结果安全占位 / md+json 附件头 e2e / 非法参数 400+404 / bundle zip 内容校验 / 页面导出 UI）。
  - 全量 pytest **696 passed / 0 failed / ~249s**（Phase 55 净增 7 例）。
  - 收口：编排结果从「只能在线看」到「一键带走」——可下载、可打包、可粘贴进文档/IM，与 Phase 47 HTML 报告、Phase 53 产物归档共同构成完整交付闭环。


- **Phase 56 关键落地（编排结果 A/B 对比 · 改动带来了什么）**：
  - `superagent.py`：`_diff_snapshot()` 压平关键指标（耗时/自检分/伙伴成功/护栏告警/冲突/LLM 调用/tokens/成本/路由/产物/伙伴列表）+ `_DIFF_METRICS` 方向表（耗时·成本·tokens·调用·告警·冲突 = 越低越好；自检分·伙伴成功 = 越高越好）+ `diff_runs(ts_a, ts_b)` 返回结构化 diff（metrics 带 delta/improved、routed 与 artifacts 的 added/removed/same、partners 逐名配对含 changed/only_in、goal_changed、verdict 改善/退化/持平/有得有失）。
  - API：`GET /api/superagent/diff?a=&b=`（JSON diff，缺参 400 / 缺记录 404）、`GET /api/superagent/diff/report?a=&b=`（自包含 HTML 左右分栏对比报告，内联 CSS，结论徽章 + 指标表 + 伙伴逐项 + 路由/产物变化 chip，目标不一致时给警示）。
  - `superagent.html` 新增「🔀 编排对比」卡：A(基线)/B(对照) 两个下拉（自动预填倒数第二次 / 最近一次，轮询刷新保留已选项）+ 对比按钮 + 对比报告新窗口链接；结果区渲染结论徽章 / 目标 A·B / 指标表（Δ 列按改善绿·退化红）/ 伙伴逐项（仅A·仅B·状态变化·一致）/ 路由域与产物 added·removed·same。
  - 测试：`tests/test_phase56_diff.py` 8 例（指标方向与改善判定 / 退化结论 / 有得有失+持平 / 路由与产物增减 / 伙伴配对与顺序 / 缺记录 None / API 200·400·404 + HTML 报告 / 页面 UI）。
  - 全量 pytest **704 passed / 0 failed**（Phase 56 净增 8 例）。
  - 收口：从「单次结果只能单看」到「两次结果可横比」——改了提示词/换了模型/调了参数之后，一眼看出是改善还是退化。


- **Phase 57 关键落地（编排结果人工批注 · 评论沉淀进记忆）**：
  - `superagent.py` 批注层：`_ANNOS` + `outputs/superagent_annos.json` 持久化（沿用 Phase 46 webhook 的 `_load_*`/`_save_*`/`_ANNOS_LOADED` 套路）；`add_annotation(ts, text, author, rating, tags, sediment, base_dir)`（内容非空与 ts 校验、rating 越界/非数字归 None、tags 去空去重保序上限 8、正文截 4000）、`list_annotations(ts)`（按 ts 过滤 + created_at 升序）、`remove_annotation(id)`、`get_annotation_stats(ts)`（条数/评分条数/均分/标签分布按次数降序/已沉淀数）。id 单调递增不复用（避免历史引用错位）。
  - **沉淀闭环**：默认 `sediment=True` 时把批注 `absorb()` 进记忆图谱，goal 前缀 `【人工批注】` 带上原编排目标，正文附评分；记录 `sedimented` 与 `entities_added/relations_added/facts_count`。**沉淀失败不阻断批注落库**（批注本体优先，异常只写进 `sediment.error`）。
  - API：`GET /api/superagent/annotations?ts=`（列表+统计）、`POST .../annotations/create`（空内容/缺 ts → 400）、`POST .../annotations/delete`（未找到 → 404）。
  - `superagent.html` 结果面板新增「✍️ 人工批注」卡：多行正文 + 星级评分下拉 + 标签（中英文逗号分隔）+ 署名 + 「沉淀进记忆」勾选（默认开）+ 提交按钮；批注列表按时间升序显示署名/星级/时间/沉淀计数（+N实体/M关系，失败显原因）/标签/删除按钮；卡片标题右侧实时统计（N 条 · 均分 · 已沉淀 M）。
  - 测试：`tests/test_phase57_annotations.py` 19 例（落库与 id 递增 / 空内容缺 ts 校验 / rating 归一 9 组参数化 / 标签上限去重 / 沉淀成功 / 沉淀失败不阻断 / 列表过滤+升序 / 删除命中与未命中 / 统计均分标签分布 / JSON 落盘重载 / API create-get-delete e2e + 400/404 / 页面 UI）。
  - 全量 pytest **723 passed / 0 failed**（Phase 57 净增 19 例）。
  - 收口：编排结果从「机器自评」到「人机共评」——人工批注不只挂在页面上，而是真回流进记忆图谱，下次同类目标编排时会被 `recall` 召回。


- **Phase 58 关键落地（编排回放 · 同目标重跑 + 血缘追踪 + 自动对比闭环）**：
  - `superagent.py` 血缘基建：`SuperAgent.__init__` 加 `self.replay_of`；`_run_core` 成功/失败两个 result dict 均写 `replay_of`；`_record` 的 summary 同步带 `replay_of`（原始编排为空串，供列表/统计区分）。
  - 回放三件套：`replay_run(ts, base_dir, llm_call, session_id, model, quality_gate, queue_wait_sec)` —— 取原编排 goal 重跑（model 未显式指定时继承原编排，session_id 默认 `replay:<ts>`），busy 透传，原记录不存在 → None，原 goal 空 → ok=False；`list_replays(source_ts)`（按源过滤 + ts 升序，原始编排不进列表）；`get_replay_lineage(ts)`（双向视角：从源看挂 N 次回放，从回放看指向源编排）。
  - API：`POST /api/superagent/replay {ts, model?, quality_gate?, queue_wait_sec?}`（缺 ts 400 / 无记录 404 / 排队超时 429）、`GET /api/superagent/replays?ts=`、`GET /api/superagent/lineage?ts=`（缺 ts 400 / 无记录 404）。
  - `superagent.html`：历史行加「🔁 回放」按钮；结果面板导出卡内嵌 `lineageBox` 血缘条（是回放 → 显示源 ts + 原目标；是源 → 列出历次回放可点击回看）。
  - **闭环**：`replayRun()` 回放成功后自动刷新历史 → 把 A=原 ts / B=回放 ts 填进 Phase 56 的对比下拉 → 直接 `runDiff()` 并滚动到对比区，重跑完立刻看到改善还是退化。
  - 测试：`tests/test_phase58_replay.py` 9 例（重跑+血缘标记 / 缺记录 None·空 goal / busy 透传 / model 继承与覆盖 / `_record` summary 血缘（回放带 ts·原始为空串）/ 回放列表过滤与升序 / 血缘双向 / API 200·400·404 + replays + lineage / 页面 UI + 自动对比闭环）。
  - 全量 pytest **732 passed / 0 failed**（Phase 58 净增 9 例）。
  - 收口：与 Phase 55 导出、56 对比、57 批注串成完整实验闭环 —— **导出结果 → 写批注 → 改提示词/换模型 → 一键回放 → 自动 A/B 对比**，改得好不好当场见分晓。


- **Phase 59 关键落地（批注驱动的自优化 · 低分反馈 → 改写目标 → 优化回放）**：
  - `superagent.py` 反馈层三件套：
    - `collect_feedback(base_dir, min_rating, limit)` —— 批注 x 编排 join（经 `_load_persisted`），每条带 goal/ok/score/routed/elapsed；`min_rating` 过滤；编排已被归档的「孤儿批注」保留（goal 空串）。
    - `analyze_feedback(base_dir, low_rating_max)` —— 均分/已评数/低分数（`low_rating_max` 钳 1-5）；**标签惩罚榜**（tag → 次数/低分次数/均分/低分率，按低分次数→总次数→名称排序）；**域统计**（路由域同口径）；低分目标列表；**纯规则建议**（均分<3 点名整体、标签/域低分率≥50% 或低分≥2 点名专项、最差 3 条低分目标提示可优化重跑；无负面模式给中性建议）。
    - `build_optimized_goal(ts, base_dir, llm_call, max_feedback)` —— 无批注 → 原样回放（`method:"none"`）；有批注 → 规则拼接（原目标 + 【历史人工反馈 · 请针对性改进】文本块，`render_feedback_block` 评分头+标签+正文截 400 字、条数上限 8）；**LLM 模式**可选（提示词优化器把批评合成可执行约束，输出过短 `len < max(10, len(goal)//3)` 或异常自动回退规则模式）。
  - `replay_run(..., optimize=False)`（Phase 59 扩展）：`optimize=True` 时不用原 goal，改用优化后目标跑；返回增加 `original_goal / optimized / method`；优化失败透传 `ok:False`。
  - API：`GET /api/superagent/feedback?low_rating_max=`（分析+建议）、`GET /api/superagent/feedback/optimize?ts=`（优化后目标预览，不执行；缺 ts 400 / 无记录 404）、`POST /api/superagent/replay` 透传 `optimize` 布尔。
  - `superagent.html` 新增「🧪 反馈自优化」卡：统计 pills（批注/已评/均分红绿/低分数）+ 规则建议列表 + 标签惩罚榜（低分率红绿）+ 低分目标行内「🧪 优化并重跑」按钮；`optimizeReplay(ts)` 先拉预览弹确认（显示方法/反馈数/目标正文前 600 字）再真实回放，完成后自动把 A=原 / B=新 填进对比下拉并 `runDiff()` 滚动到位。
  - 测试：`tests/test_phase59_feedback.py` 11 例（join 与 min_rating / 孤儿批注 / 统计与标签惩罚榜与域 / 钳位与空态 / 反馈块渲染 / 规则模式 / 无批注与缺记录与空 goal / LLM 改写与过短回退与异常回退 / replay optimize / API e2e / 页面 UI）。
  - 全量 pytest **743 passed / 0 failed**（Phase 59 净增 11 例）。
  - 收口：自优化闭环成型 —— **批注评分 → 反馈分析（标签/域惩罚榜+建议）→ 一键生成优化目标 → 优化回放 → 自动 A/B 对比 → 新批注再回流**，人的批评直接变成下一轮的约束。


- **Phase 60 关键落地（编排质量基线 · 统计分布 + 偏离自动告警）**：
  - `superagent.py` 基线层（disk-only，与 trend/digest 同口径）：
    - `_baseline_rows(base_dir, days)` —— 时间窗内编排摘要映射（score/elapsed/partners_ok），非法 ts（epoch=0）与窗外记录剔除；
    - `_bl_stats` 均值/总体标准差/min/max/空态；`_bl_deviations(me, peers, zt)` —— 三指标（自检分低=坏 / 耗时高=坏 / 伙伴成功低=坏）按 z 分数判坏方向超阈；**std==0 时用 max(1%|mean|, 1.0) 伪标准差兜底**（规则式编排分数高度一致 std 恒 0，不兜底永远检不出偏离）；`|z| ≥ 1.5×阈值 → high，否则 medium`；
    - `get_quality_baseline(days, min_runs)` —— 全局分布 + 按 goal 分组分布（runs ≥ min_runs 才入组，按 runs 降序）；
    - `check_quality(ts, z, min_runs)` —— 单次编排 vs 同目标基线（peers 不含自身），不足 min_runs → `verdict:"insufficient"`（带 have/need）；
    - `list_quality_alerts(days, z, min_runs, limit)` —— 全窗扫描组内偏离，稳定双排序（先 ts 倒序再 high 优先）。
  - API：`GET /api/superagent/quality/baseline?days=&min_runs=`、`GET /api/superagent/quality/check?ts=&z=`（缺 ts 400 / 窗内无记录 404）、`GET /api/superagent/quality/alerts?days=&z=`。
  - `superagent.html` 新增「📏 质量基线」卡：全局 pills（近 N 天编排数/自检分 mean±std/耗时/伙伴成功/可比目标组）+ 按目标基线前 5 组 + 偏离告警列表（每条：时间可点回看详情 + 目标截断 + 红黄偏离徽章「自检分 40 (基线 90±0, z=-50)」+ 行内 🔁 回放按钮）。
  - 测试：`tests/test_phase60_baseline.py` 8 例（时间窗与字段映射 / 统计数学 / 基线分组与 min_runs / 偏离方向+伪 std+severity / check 四路径 / 告警扫描与排序 / API e2e / 页面 UI）。
  - 全量 pytest **751 passed / 0 failed**（Phase 60 净增 8 例）。
  - 收口：回放从「跑一次看一眼」升级为「有基线可依」—— 同一目标的第 N 次运行自动对照前 N-1 次的分布，自检分/耗时/伙伴成功率任何坏方向跳变当场亮红黄牌，并可直接一键回放验证。


- **Phase 61 关键落地（质量告警接入 Webhook · 飞书/钉钉卡片推送）**：
  - `superagent.py`：`add_webhook` events 白名单扩为 `all|done|fail|quality`（非法值仍归 all）；`_panel_url_for(path)`（复用 `_PUBLIC_BASE_URL` 生成面板页链接）；`_quality_md(payload)`（窗口+告警数 + 每条 ts/goal/偏离指标行「自检分 **40** (基线 90±0, z=-50)」+ 面板链接，飞书/钉钉共用）；`_webhook_wrap` 新增 quality 分支（feishu 红头 interactive 卡片带「查看质量基线」按钮 / dingtalk markdown+actionCard / raw 原样）；`push_quality_alerts(days, z, min_runs, limit)` —— 扫描 `list_quality_alerts` 后向 `events in (all, quality)` 的启用接收端推送，**无告警 skipped=True 静默不发**（防空跑刷屏）。
    🔴 坑：`push_quality_alerts` 定义在文件前部（Phase 46 区段），默认参数引用了文件尾部 Phase 60 的 `_BASELINE_Z`/`_BASELINE_MIN_RUNS` → def 时求值 `NameError`。改为 `z=None, min_runs=None` 调用时再解析。
  - API：`POST /api/superagent/quality/push {days?, z?, min_runs?, limit?}`。
    🔴 坑复发：POST 路由又挂进了 `do_GET` 块 → 404（与 Phase 50 digest 同款错误）。**铁律升级：每次加路由先 grep 确认块位置，POST 一律进 `do_POST` 分发段。**
  - `superagent.html`：webhook 表单事件下拉加「仅质量告警」选项；「📏 质量基线」卡加「📢 推送告警」按钮（确认 → 推送 → 结果提示：skipped 提示无告警未发送）。
  - 测试：`tests/test_phase61_quality_push.py` 7 例（events 白名单 / _quality_md 内容 / 三格式包装 / 无告警 skipped + 有告警无接收端 / 本地捕获服务真实送达 + events 过滤 + panel_url 注入 / API e2e / 页面 UI）。
  - 全量 pytest **758 passed / 0 failed**（Phase 61 净增 7 例）。
  - 收口：质量告警从「面板里看」到「主动找人」—— 偏离一出现即可推到飞书/钉钉，与 Phase 49 富文本卡片、Phase 60 基线打通成完整监控链。


- **Phase 62 关键落地（告警静默期 + 调度器自动推送）**：
  - `superagent.py`：
    - `push_quality_alerts(..., silence_hours=0)` —— 按 goal 静默去重：窗口内已推送过的 goal 本次跳过（`silenced` 计数）；**只在 sent>0 时记账**（没送出去不吞告警）；**全部被静默/无告警时直接跳过发送**，不发 `count=0` 空 payload；`silence_hours=0` 手动强推可重复发送。
    - 静默状态持久化 `outputs/superagent_quality_state.json`（`{last_push_epoch, silence:{goal:epoch}}`），重启不丢；`_load/_save_quality_state`。
    - 自动推送：`set_quality_auto(enabled, interval_h, silence_h)`（interval≤0 强制关）+ `get_quality_auto` + `_maybe_auto_quality()`（派发前先更新 `last_push_epoch` 防 tick 密集重复派发，推送失败不回滚——下个间隔再试的"尽力而为"语义）+ `_push_quality_safe`（带 silence_h）。
    - `_scheduler_tick` 挂入 `_maybe_auto_quality`（每 tick 轻量时间检查）。
  - `web/server.py`：`run_web` 启动注入 config `agent.quality_auto_push` + `agent.quality_auto_interval_h`（并加载静默状态）；API `GET /api/superagent/quality/auto` + `POST .../quality/auto {enabled,interval_h?,silence_h?}`；`POST .../quality/push` 透传 `silence_hours`。
  - `superagent.html`：质量基线卡加「⏰ 自动推送: 关/每24h」切换按钮（开启前确认，说明 24h 间隔+静默语义）。
  - 测试：`tests/test_phase62_silence_auto.py` 10 例（静默去重+手动强推 / 未送达不记账 / per-goal 隔离 / 状态落盘重启生效 / set_quality_auto 语义 / 自动推送三路径 / 真实送达 / scheduler 钩子 / API / 页面 UI）。
    🔴 坑：首版静默过滤后仍发 `count:0` 空 payload（捕获服务收到第 2 次空推）。修正为空告警直接 return skipped=True。**"过滤后为空就别发"——告警类推送必须以最终告警集为准判空，不能只看原始集。**
  - 全量 pytest **768 passed / 0 failed**（Phase 62 净增 10 例）。
  - 收口：告警从「手动推」到「自动盯」—— 调度器每 tick 检查间隔，到期自动扫描+推送，同 goal 24h 静默防轰炸；可 config 开机即用（`agent.quality_auto_push = true`）。


- **Phase 63 关键落地（设置中心并入编排/通知配置 · 改完即生效）**：
  - `web/server.py`：`_SETTINGS_SCHEMA` 新增「编排与通知」组 5 键 —— `agent.public_base_url`(string, 通知内嵌链接) / `agent.digest_time`(string, HH:MM 日报时刻) / `agent.orchestration_retry_max`(int, 0-5) / `agent.quality_auto_push`(bool) / `agent.quality_auto_interval_h`(float 小时)；全部 `restart: False`（运行期开关）。
  - **软生效闭环**：`_settings_save` 软重载 `_RUNTIME_CONFIG` 后，即时重注入 `set_digest_time / set_default_retry_max / set_public_base_url / set_quality_auto` —— 设置页保存后无需重启面板立即生效（此前这些键只在 `run_web` 启动时注入一次）。
    🔴 坑：注入代码写在 `_settings_save` 里却没导入 `_sa`（server.py 的 `_sa` 都是方法内局部导入），NameError 被周边 try/except 吞掉 → 保存"成功"但四项全部没生效。**silent except 是双刃剑：吞异常保服务的同时也吞了 bug，被吞的路径必须有测试覆盖**（本例正是 e2e 断言运行期状态才暴露）。
  - settings.html 零改动 —— 本就 schema 驱动渲染，新组自动出现在表单。
  - 测试：`tests/test_phase63_settings_notify.py` 4 例（schema 组与类型 / GET values 含新键 / form 保存 e2e：config 落盘 + 运行期状态即时生效 + 再关闭 / 页面 schema 驱动）。
  - 全量 pytest **772 passed / 0 failed**（Phase 63 净增 4 例）。
  - 收口：质量告警/日报/重试/公开地址全部进设置中心统一管理，改完即生效，运营不再需要碰 config.toml 或重启。


- **Phase 64 关键落地（编排成本预算护栏 · 单日超限自动暂停定时编排）**：
  - `superagent.py`：`_BUDGET` 状态（daily_limit/paused/paused_at/alerted_date）+ `today_cost(base_dir)`（disk-only 按 ts 前缀匹配今天，昨日不计）+ `set_daily_budget(limit)`（≤0 不限并解除暂停）+ `get_budget_state()`（over 判定 + **不再超限自动解除暂停**——次日成本归零天然恢复）。
  - **护栏语义**：超预算只挡「定时编排」的自动派发（`_scheduler_tick` 跳过到期计划），手动编排/回放不受限——保住人的操作自由，管住无人值守的失控；`paused` 置位 + 记录 `paused_at`；**同日只推一次**预算告警（`alerted_date` 去重），推 `events in (all, budget)` 接收端。
  - 通知：`push_budget_alert` + `_budget_md`（预算/已花费/处置说明/成本看板链接）+ `_webhook_wrap` budget 分支（feishu 橙头卡片 / dingtalk markdown+actionCard）；`add_webhook` events 白名单再扩 `budget`。
  - `web/server.py`：settings schema「编排与通知」组加 `agent.daily_budget`(float)；`run_web` 启动注入 + `_settings_save` 软重载后重注入；API `GET /api/superagent/budget`（状态）+ `POST .../budget {daily_limit}`。
  - `superagent.html`：定时编排卡标题加 `budgetChip`（💰今日 ¥x / 预算 ¥y，超限红字/暂停黄字）。
  - 测试：`tests/test_phase64_budget.py` 9 例（today_cost 只算今天 / set 语义 / over 判定与自动解除 / tick 超限跳过派发+暂停标记 / tick 正常派发+告警同日一次 / 真实送达+事件过滤+三格式 / API / schema / 页面）。
  - 全量 pytest **777 passed / 0 failed**（Phase 64 净增 9 例）。
  - 收口：成本治理从「事后看板」到「事前护栏」—— 预算内自由跑，超限自动刹车并主动告警，次日自动恢复，无人值守编排不再有失控烧钱风险。
